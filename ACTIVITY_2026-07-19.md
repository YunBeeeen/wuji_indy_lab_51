# ACTIVITY 2026-07-19

## v1.1(keypoint) 판정: 기각 — 매달림은 보상이 아니라 파지 기하 문제

- run `2026-07-18_22-48-57` (keypoint φ + ori 15°), iter 6,607: success 0, 자세 오차
  1.2 rad(70°) 횡보, 낙하 4.6%(안정), error_pos 0.119 (12cm 접근).
- 결정적 단서: ori 조건·보상이 다른 슬롯 B도 종료 자세 1.16 rad로 동일 → 기울임은 정책
  선택이 아니라 역학 (케이지 파지의 중력 토크 매달림). play 관찰(사용자): 손가락 2개
  불안정 파지 = 2점 접촉 경첩.
- 인과 사슬 완성: v1 "유인이 없다" → v1.1 "유인은 있는데 수단이 없다" → v2 = 파지 기하.
- depth_max 증량 검토 후 기각 (수박씨 배출 + 점접촉 마찰은 토크에 무력).

## 감싸쥐기 cage 부품 준비 (미배선)

- `BOX_CAGE_BODIES_WRAP` (box_mdp_cfg.py): 폭(약지/새끼 tip+link3) + 깊이(검지/중지
  link2) 확장, 11 bodies. 활성화 = `BOX_CAGE_BODIES = BOX_CAGE_BODIES_WRAP` 한 줄.
- 활성 시 gate 값 하락(점 평균 희석) → gate_threshold 0.3 재보정 필요 주석 명기.
- 링크 존재 확인: indy7_wuji_right.urdf에 finger*_link1/2/3 + tip_link.

## 스틱 라운드 계획 (사용자 확정)

- 순서: ① 스틱 기준선 (단면 1.5~3cm × 길이 18~20cm, 현행 손끝 cage, ori 없음 — CLI
  오버라이드 런치) → ② cage 개편(WRAP) fresh 비교 → ③ 개편 승리 시 단면 1cm 확장.
- 1cm는 실측 밖(손끝 간격 2.8cm 하한)이라 감싸쥐기 준비 후 도전.

## 감싸쥐기(WRAP) 활성화 + 새 라운드 구성 (사용자)

- `BOX_CAGE_BODIES = BOX_CAGE_BODIES_WRAP` 활성 (11 bodies — 폭: 약지/새끼, 깊이: link2).
  모든 gate/hold/reach/success에 일괄 적용. 스모크 통과.
- 라운드 구성:
  - **슬롯 A**: 기존 크기(3~6cm × ratio 1.5~3, 파일 기본) + WRAP + ori 15° + keypoint φ
    — "감싸쥐기가 매달림을 고쳐 ori success를 여는가". 대조군 = v1.1(22-48-57, 손끝 cage).
  - **슬롯 B**: 얇은 스틱(1.5~3cm × 18~20cm) + WRAP + ori 없음
    — "감싸쥐기로 얇은 것을 잡을 수 있는가". 1cm 확장은 이 결과 후.
- 관찰 포인트: gate 값 희석(5→11 bodies) → Episode_Reward_Raw/finger_cage_hold 레벨이
  이전 런 대비 낮게 시작하는 게 정상. success 발화가 지나치게 안 되면 gate_threshold
  0.3 하향 검토. 짧은 물체 부분 감점은 슬롯 A(4.5cm~)에서 유의.

## WRAP 라운드 첫 판독 (iter ~1,150) + 스틱 파지 실패 원인 실측

- 런 정체 (스냅샷 검증): `12-45-27` = 슬롯 A (WRAP + ori 15° + 기존 3~6cm),
  `12-48-25` = 슬롯 B (WRAP + ori 없음 + 스틱 1.5~3cm). 둘 다 WRAP 확인됨.
- **슬롯 A 청신호**: box_ori_error 0.94→0.73 첫 하락 (v1.1은 1.2 횡보) — 감싸쥐기가
  매달림을 잡기 시작. max clearance 0.26, 계속 관찰.
- **슬롯 B 파지 실패 원인 = hand_floor 충돌 (실측)**: 스틱 파지 지점(상판 위 0.75~1.5cm)이
  hand_floor 페널티 존(2cm) 안 → "잡으러 가라"(hold) vs "가지 마라"(hand_floor) 충돌.
  증거: hand_floor raw 악화(−0.77→−0.82), 중지 접근 정체(0.14), cage_sdf_min 관통 0(+),
  clearance 0.005 (들기 전무), displacement 0.096 (밀고만 다님).

## 코드 수정 (재학습은 사용자, 코드만 반영)

- `isaac_neuromeka/tasks/manipulation/grasp/box_mdp_cfg.py` — `hand_floor.params.clearance`
  0.02 → **0.01**: 스틱 파지 높이(≥0.75cm)를 합법화. 근거 주석 동봉.
- `isaac_neuromeka/mdp/commands.py` — `UniformCubeGoalCommandCfg.goal_marker_cfg`:
  구슬(Sphere r=0.02) → **반투명 직육면체** (0.025×0.10×0.025, opacity 0.35, y로 길쭉).
  goal "자세"(월드 정렬)가 보이도록 — 마커 자세 identity = 목표 자세 그대로.
  v2(goal 자세 랜덤화) 시 `_debug_vis_callback`에 orientations 전달 확장 주석 포함.
  ※ 이 마커는 큐브 태스크에도 공유됨 (시각 전용이라 무해).
- 4 env 스모크 통과 (마커 생성 포함), 스모크 폴더 정확 삭제.
- 슬롯 B 재런치 명령 (hand_floor 0.01은 파일 기본이라 기존 인자 그대로):

```bash
python scripts/rsl_rl/train.py --task Indy-Wuji-Box-Transport --headless \
  "env.events.randomize_box.params.width_range=[0.015,0.03]" \
  "env.events.randomize_box.params.length_range=[0.18,0.20]" \
  env.terminations.success.params.ori_limit=null
```
