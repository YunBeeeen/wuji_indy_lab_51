# 2026-07-10 Activity Log

- 이 문서는 2026-07-10 작업 내용과 판단을 날짜별로 기록한 활동 일지 문서임.

## 전체 요약

- 오늘은 `Indy-Wuji-Cube-Grasp` task를 구성함.
- 기존 `Indy-Wuji-Reach` baseline은 보존함.
- cube grasp는 `manipulation/grasp` package로 분리함.
- 처음에는 grasp success가 아니라 cube 접근 baseline으로 시작함.
- 중간에 arm + five-finger action 구조까지 확장했으나, 논문식 구현 우선순위에 맞춰 최신 구조는 arm + thumb/index/middle action으로 되돌림.
- 최신 구조는 reach -> lift -> lifted object-goal tracking 흐름임.
- TensorBoard에 reward와 실제 거리 metric을 같이 보이게 함.
- 4096 env run 중 PhysX patch buffer overflow를 확인하고 buffer를 키움.

## Task 구조

- 새 task id는 `Indy-Wuji-Cube-Grasp`임.
- task package는 `isaac_neuromeka/tasks/manipulation/grasp/`임.
- 공통 cfg는 `cube_grasp_env_cfg.py`임.
- Indy/Wuji override는 `grasp/indy_wuji/env_cfg.py`임.
- RSL-RL experiment name은 `indy_wuji_cube_grasp`임.
- task는 active command 없이 cube state 기반으로 시작함.

## Scene

- `CubeGraspSceneCfg`는 기존 reach scene을 재사용함.
- cube는 `{ENV_REGEX_NS}/Cube`에 `RigidObjectCfg`로 추가함.
- cube size는 `0.06 m`임.
- cube mass는 `0.08 kg`임.
- cube initial position은 `(0.45, -0.18, 0.03)`임.

## Action

- 초기 arm-only smoke test는 action 6D였음.
- 중간 단계에서 arm + thumb/index/middle 18D로 확장함.
- 최신 구조는 arm + thumb/index/middle 18D임.
- controlled joint regex는 `joint[0-5]`, `finger[1-3]_joint[1-4]`임.
- 기존 26D checkpoint는 18D 구조에 바로 load할 수 없음.

## Observation

- 최신 policy observation은 57D임.
- `joint_pos`는 controlled joints 18D임.
- `cube_pos`는 `cube_pos_w - palm_link_pos_w` 3D임.
- `cube_in_fingertips`는 five-fingertip 기준 cube relative vector 15D임.
- `cube_to_goal`은 cube target relative vector 3D임.
- `action_history`는 previous action 18D임.
- 현재 joint velocity, fingertip velocity, contact state는 observation에서 뺌.

## Reward

- 현재 reward는 완성된 grasp success reward가 아님.
- 현재 reward는 reach, functional hold/cage, lift, lifted object-goal tracking shaping reward임.
- active reward term은 `arm_cube_reach`, `finger_cube_reach`, `functional_hold`, `cube_lift`, `cube_goal_tracking`, `action_rate`임.
- `arm_cube_reach`는 `palm_link`와 cube root 거리 기반임.
- `finger_cube_reach`는 finger1~3 tip과 cube root 사이 weighted distance의 best-so-far progress 기반임.
- `finger_cube_reach` weight는 `0.3`임.
- `finger_cube_reach` body weight는 `3.0/1.0/1.0`임.
- `finger_cube_reach`는 현재 `mode="best"`임.
- `finger_cube_reach` raw 값은 절대 거리가 아니라 최단거리 갱신량임.
- `functional_hold`는 Dexterous Functional Grasp 참고용 hold/cage reward임.
- `functional_hold`는 cube가 thumb/index/middle tip 사이 grasp region에 들어오는지 봄.
- `functional_hold` weight는 `0.2`임.
- `cube_lift`는 cube root z height 기반임.
- `cube_goal_tracking`은 cube z가 `0.08 m` 이상일 때만 켜지는 target position reward임.
- cube goal position은 `(0.55, -0.05, 0.12)`임.
- `action_rate` weight는 `-0.0003`임.
- contact reward는 아직 없음.

## TensorBoard

- weighted reward는 `Episode_Reward/*`임.
- raw reward는 `Episode_Reward_Raw/*`임.
- 실제 cube 거리 error는 `Metrics/cube/*`임.
- `Metrics/cube/palm_distance`는 palm-cube 거리임.
- `Metrics/cube/thumb_distance`는 thumb-cube 거리임.
- `Metrics/cube/index_distance`는 index-cube 거리임.
- `Metrics/cube/middle_distance`는 middle-cube 거리임.
- `Metrics/cube/ring_distance`는 ring-cube 거리임.
- `Metrics/cube/little_distance`는 little-cube 거리임.
- `Metrics/cube/finger_mean_distance`는 five-fingertip 평균 거리임.
- `Metrics/cube/non_thumb_mean_distance`는 index/middle/ring/little 평균 거리임.
- `Metrics/cube/finger_weighted_mean_distance`는 `3/1/1/1/1` weight 기준 평균 거리임.

## PhysX

- 4096 env cube grasp run에서 `Patch buffer overflow detected` 에러가 발생함.
- 처음 요구치는 약 `171k` patch였음.
- 이후 resume run에서 약 `263k`까지 올라감.
- `2**18 = 262144`는 부족했음.
- 최신 설정은 `gpu_max_rigid_patch_count = 2**19`임.
- 이 설정은 새 train/resume 실행부터 반영됨.

## 실행 결과

- `Indy-Wuji-Cube-Grasp --headless --num_envs 1 --max_iterations 1` smoke test는 통과함.
- 최신 18D/57D/lift-goal 구조 smoke test도 통과함.
- 최신 smoke test log dir은 `logs/rsl_rl/indy_wuji_cube_grasp/2026-07-10_21-17-55`임.
- GUI play에서 finger가 cube 근처로 접근하는 것을 확인함.
- `finger_cube_reach` raw reward는 논문식 progress reward로 변경됨.
- 변경 후 smoke test에서 `Episode_Reward_Raw/finger_cube_reach`가 낮게 보이는 것은 정상임.
- 실제 접근 여부는 `Metrics/cube/*` 거리 metric으로 봄.
- `Metrics/cube/finger_mean_distance`는 약 `0.05~0.06 m` 수준까지 내려감.
- progress reward 변경 후 smoke test를 다시 실행함.
- progress reward smoke test에서 action shape 18, policy observation shape 57, reward term 5개 확인함.
- progress reward smoke test log dir은 `logs/rsl_rl/indy_wuji_cube_grasp/2026-07-10_21-31-26`임.
- `functional_hold` 추가 후 smoke test를 다시 실행함.
- `functional_hold` smoke test에서 action shape 18, policy observation shape 57, reward term 6개 확인함.
- `functional_hold` smoke test log dir은 `logs/rsl_rl/indy_wuji_cube_grasp/2026-07-10_21-41-01`임.
- random policy smoke test에서는 `functional_hold Raw`가 0으로 나왔고, 이는 cube가 아직 grasp region 안에 들어오지 않았다는 뜻임.
- 현재 reward는 cube를 잡는 것까지는 유도하지 못함.
- 다음 단계는 contact/lift reward 설계임.

## 다음 판단

- 현재 접근 baseline은 동작함.
- 단순 거리 reward만 오래 돌리는 단계는 지났음.
- 다음 구조에서는 palm/finger 배치, contact, lift 조건을 넣어야 함.
- palm이 cube를 보게 하는 직접 orientation reward보다 cube frame 기준 fingertip 배치와 contact 조건을 먼저 검토함.
- ring/little은 현재 action/reward 핵심이 아니라 observation/metric 보조로 둠.
- 다음 구현은 contact group reward와 contact-gated lift/goal 구조임.
