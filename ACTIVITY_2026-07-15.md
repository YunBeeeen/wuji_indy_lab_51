# 2026-07-15 Activity

## 아침: 밤샘 런 결과 확인 + r_T 도입

- 밤샘 런이 캠핑 국소최적을 탈출해 큐브를 들었음 (스크린샷 확인).
- 자세는 palm-up scoop임: 손바닥을 뒤집어 밑에서 받침. 중력이 파지력을 대신함.
- 해킹은 아님 (최하 꼭짓점 clearance 기준 실제 lift). 팔 접힘/비-pinch 스타일이 남은 문제임.
- r_T를 구현함: `ObjectLiftedHeld` 성공 종료 (clearance>8cm AND gate>0.3, 15스텝 유지)
  + `lift_success` 한 방 보상 (weight 15000 = 로그 스케일 +500) + 즉시 종료.
- 다음 런부터 적용됨. 이번 런의 가중치는 hold 15 / lift 100임 (사용자 변경 반영).
- 열린 결정: 재학습/resume 여부, scoop 스타일 수용 여부.

## 오전 2: 테이블 도입

- 팔꿈치 웅크림의 원인을 task 기하(바닥 큐브)로 보고 테이블(BASE_Z=0.40)로 검증 실험함.
- Support kinematic cuboid를 추가하고 큐브를 상판(z=0.43)에 올림. 하강량 60→17cm.
- cube_lift / hand_floor / success 전부 surface_z=BASE_Z로 배선함 (metrics는 자동).
- r_MP는 scoop에서 데드존(manip 0.35~0.5 ≫ j_max 0.02)임을 확인함 → 자세 손잡이에서 제외.
- 스모크 테스트 통과함 (action 18 / obs 57, success 종료항 등록).
- (후속) A/B 통제를 위해 r_T를 주석처리함 (사용자 지적). 전 런과의 차이는 테이블 + arm_floor 둘로 좁힘.
- arm_floor 페널티를 추가함: link[2-6]이 바닥 0.12m 아래로 내려가면 감점 (실측 기반 링크 선정).
- 스모크 테스트 통과함 (종료항 time_out 하나, action 18 / obs 57).
- cube_dropped 낙하 종료를 추가함 (상판 -5cm 아래로 떨어지면 리셋, BASE_Z 파생 배선).
- 편집 중 삭제됐던 surface_z 배선을 복구함 (없으면 lift가 스폰부터 만점인 버그).
- 최근 10350 iter 런의 episode_length는 8.0초였음을 확인함.

## 오후 2: 운반 task 구현 (다음 런용)

- 방향 결정: 랜덤 goal 운반 → (grip 스크립트 검증 병행) → 젓가락. orientation은 제외함.
- UniformCubeGoalCommand + 운반 차분층(500) + goal 기반 r_T(+500, 즉시 종료)를 구현함.
- 기존 cube_to_goal 관측의 env_origins 미보정 버그를 수정함 (obs 57 / action 18 불변).
- commands.py 전방 참조 NameError로 play가 잠시 안 열렸음 → 수정함. 스모크는 학습 종료 후.
- (후속) 커리큘럼 1단계로 goal을 스폰 위 +20cm 고정점으로 축퇴함. 2단계 확장값은 주석 보존.

## 저녁: 1단계 실행 방침

- fresh 대신 테이블 런 체크포인트 파인튜닝으로 결정함 (전이 이득, 고정 goal이라 관측 의미 변화 무해).
- "transport 하강 감점이 lift를 억제한다" 우려는 수치로 기각함 (실패 사이클도 순익 +7).
  감시 지표는 cube_lift raw 하락 여부, 헤지는 transport 500→200임.
- goal_radius 5cm는 시작값으로 유지함. 정밀도는 성공 후 3cm로 조임. 동심원 절대 보상은
  서성임 연금이라 기각하고, 필요 시 포텐셜 볼록화(1/(0.05+d))로 함.
- 보상 부호 분리: cube_transport를 best-so-far(+전용)로, 낙하 비용은 drop_penalty(−100 정액)로
  이사함. 스모크는 사용자가 돌림.
