# 2026-07-08 Activity Log

- 이 문서는 2026-07-08 작업 내용과 판단을 날짜별로 기록한 활동 일지 문서임.

## 전체 요약

- Isaac Sim 5.1 / IsaacLab 2.3 계열 / `env_isaaclab` 기준으로 진행함.
- 기존 Isaac Sim 4.5 / IsaacLab 2.2.1 / `chop_ws/chop_rl` 방향은 폐기함.
- active repo는 `~/wuji_indy_lab_51/nrmk_isaaclab_wuji`임.
- active task는 `Indy-Wuji-Reach`임.
- active USD는 `indy7_wuji_right_simplified.usd`임.
- 현재 목표는 full training 성능보다 env 구조 이해와 reach 학습 파이프라인 확인임.
- 현재 기준 body는 `link6`임.
- Wuji hand frame 문제는 분리하고, Indy arm flange reach baseline부터 확인하기로 함.

## Asset 정리

- Indy7 + Wuji hand 결합 asset을 사용함.
- `INDY7_WUJI_RIGHT_CFG`를 task asset config로 사용함.
- Wuji hand collision STL 26개를 USD collider로 적용함.
- `PhysicsCollisionAPI` 계열 collision schema 적용 확인함.
- Isaac Sim 5.1 URDF importer의 on-disk export 문제 확인함.
- URDF importer를 재신뢰하기보다 현재 training USD를 post-process한 상태로 사용함.

## Task 정리

- Neuromeka `Indy-Reach` 스타일의 ManagerBasedRLEnv 구조로 구현함.
- 기존 `Indy-Reach`를 덮어쓰지 않고 `indy_wuji` 새 task로 분리함.
- action은 arm 6축만 사용함.
- hand joint는 articulation에는 남아 있음.
- hand joint는 현재 policy action/observation에서 제외함.
- policy observation dim은 55임.
- action dim은 6임.
- `sim.render_interval = decimation` 적용함.

## Tracking Body 정리

- `tcp`는 현재 USD articulation body로 쓰기 부적합하다고 판단함.
- `palm_link`는 실제 body로 쓸 수 있으나 Wuji hand frame mismatch 가능성이 있음.
- `palm_link` offset 방식은 실험했으나 현재는 보류함.
- 현재는 reach 학습 확인을 위해 `link6` 기준으로 command/reward body를 바꿈.
- `link6` 기준 smoke test 통과함.

## Reward 정리

- reward term 설정은 `isaac_neuromeka/tasks/manipulation/common/env_cfg_common.py`에 있음.
- reward 계산 함수는 `isaac_neuromeka/mdp/rewards.py`에 있음.
- position reward는 body와 command position 거리 기반임.
- orientation reward는 body와 command quaternion error 기반임.
- orientation reward는 position distance가 threshold보다 크면 0으로 꺼짐.
- speed, action rate, joint velocity는 penalty임.
- 현재 reward는 `link6` 기준으로 적용됨.

## 실행 확인

- `--num_envs 1 --max_iterations 1` smoke train 통과함.
- `--num_envs 32 --max_iterations 5` headless train 통과함.
- GUI 실행 확인함.
- `128/20`, `512/100` 학습 확인함.
- 다음 긴 학습은 `4000 env / 50000 iter` 기준으로 정리함.
- `play.py` checkpoint load 확인함.
- `link6` 변경 후 smoke test 통과함.
- `link6` smoke 결과 action shape 6, policy observation shape 55 확인함.

## TensorBoard

- TensorBoard는 `logs/rsl_rl/indy_wuji_reach`를 logdir로 봄.
- 기본 포트는 `6006` 사용함.
- 포트 충돌 시 `6007` 등으로 바꿈.
- 주요 확인 항목은 position error, orientation error, position reward, orientation reward, mean reward임.

## 현재 긴 학습 명령

```bash
cd ~/wuji_indy_lab_51/nrmk_isaaclab_wuji
conda activate env_isaaclab
python scripts/rsl_rl/train.py \
  --task Indy-Wuji-Reach \
  --headless \
  --num_envs 4000 \
  --max_iterations 50000
```

## TensorBoard 명령

```bash
cd ~/wuji_indy_lab_51/nrmk_isaaclab_wuji
conda activate env_isaaclab
tensorboard --logdir logs/rsl_rl/indy_wuji_reach --port 6006
```

## 다음 할 일

- `4000 env / 50000 iter` 학습을 돌림.
- TensorBoard에서 error/reward 추세를 봄.
- `position_error`가 내려가는지 봄.
- `orientation_error`가 내려가는지 봄.
- orientation reward가 0 근처에 죽어 있지 않은지 봄.
- NaN/PhysX error 여부를 봄.
- 학습 후 GUI play로 움직임을 봄.
- `link6` reach baseline이 안정되면 Wuji `palm_link` / virtual EE offset을 다시 검토함.
