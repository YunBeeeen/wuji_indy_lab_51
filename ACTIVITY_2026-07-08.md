# 2026-07-08 Activity Log

## 전체 요약

- Isaac Sim 5.1 / IsaacLab 2.3 계열 환경 기준으로 방향 정함.
- `env_isaaclab` 환경 기준으로 실행함.
- Neuromeka public/main branch 스타일 기준으로 진행함.
- 기존 Isaac Sim 4.5 / IsaacLab 2.2.1 / `chop_ws/chop_rl` 방향은 폐기함.
- 목표는 full training 성능이 아니라 Indy7 + Wuji hand arm end-effector tracking env 구성임.
- 현재 active code dir은 `~/wuji_indy_lab_51/nrmk_isaaclab_wuji`임.
- 현재 active task는 `Indy-Wuji-Reach`임.
- 현재 active USD는 `indy7_wuji_right_simplified.usd`임.
- 현재 tracking body는 `palm_link`임.
- 현재 action dim은 arm 6축 기준 6임.
- 현재 policy observation dim은 55임.
- hand joint는 articulation에는 남기고 action/observation에서는 제외함.
- 학습 smoke test, GUI 확인, play 확인까지 됨.

## 문서 정리

- root `AGENTS.md` 생성/갱신함.
- root `WORKLOG.md` 생성/갱신함.
- root `study.md` 생성함.
- `study.md`는 코드 흐름, 환경 구성, 치명적인 주의사항 중심으로 다시 정리함.
- 내부 `nrmk_isaaclab_wuji/agent.md` 갱신함.
- 내부 `nrmk_isaaclab_wuji/worklog.md` 갱신함.
- 2026-07-08 활동 일지는 이 파일로 분리함.

## 환경 방향 정리

- Isaac Sim은 기존에 잘 켜지는 5.1 환경 사용함.
- IsaacLab은 기존 설치된 2.3 계열 환경 사용함.
- `env_isaaclab` 안의 Isaac Sim pip 패키지 기반으로 실행함.
- `/home/lsc/IsaacLab` editable 설치 상태 기준으로 진행함.
- `/home/lsc/isaacsim_pkg` standalone zip은 현재 workflow에서 직접 사용하지 않음.
- `isaac_neuromeka`는 `nrmk_isaaclab_wuji`에서 editable 설치해 사용함.
- plain `python -c "import isaac_neuromeka"`는 `pxr` 문제로 실패할 수 있음.
- IsaacLab task 실행은 `AppLauncher`를 통해 해야 함.

## Asset 작업

- Indy7 + Wuji hand 결합 asset 방향 확인함.
- `indy7_wuji_right.usd`는 full mesh baseline 성격으로 둠.
- `indy7_wuji_right_simplified.usd`를 active training asset으로 둠.
- `indy7_wuji_right_all_simplified.usd`는 fallback/debug 성격으로 둠.
- Wuji standalone URDF collision block 문제 확인함.
- `wuji_right.urdf`에서 collision block 일부가 주석 처리돼 있었던 것 확인함.
- standalone hand URDF의 collision block을 살리는 방향으로 정리함.
- Isaac Sim 5.1 URDF importer의 on-disk layered export path 문제 확인함.
- importer가 disk destination으로 내보낼 때 visuals/collisions가 비는 현상 확인함.
- in-memory import 후 Save As 방식은 정상 가능하다고 판단함.
- training용 `indy7_wuji_right_simplified.usd`는 URDF importer 재신뢰 대신 post-process 방식으로 정리함.
- Wuji hand collision STL 26개를 USD Mesh collider로 직접 넣는 방식 적용함.
- 각 collision mesh prim에 `PhysicsCollisionAPI` 계열 collision API 적용함.
- collision approximation은 convex hull 기준으로 둠.
- active hand collision mesh 26개 확인함.
- nested importer leftover는 제거/비활성 처리함.

## Collision 이해 정리

- Isaac Sim GUI의 초록/빨간 collision 표시는 view mode/selection state 영향이 있음.
- `PhysicsCollisionAPI`는 USD prim에 붙은 physics schema임.
- URDF 안에 직접 들어 있는 것이 아니라 import/post-process 후 USD prim에 붙는 정보임.
- Indy7 단독 URDF가 잘 보인다고 해서 결합 USD 전체가 자동으로 정상이라는 뜻은 아님.
- Wuji 쪽은 importer/export path와 collision authoring 상태를 따로 봐야 함.
- 학습에서 중요한 것은 GUI 색보다 USD prim에 collision API와 geometry가 실제로 있는지임.

## TCP / Body Tracking 정리

- combined URDF에는 `tcp -> palm_link` fixed joint 구조가 있었음.
- USD import/generation 과정에서 `tcp`가 articulation rigid body로 남지 않을 수 있음.
- 실제 articulation body로 사용할 수 있는 대상은 `palm_link`임.
- `tcp`는 leftover Xform/frame 성격으로 판단함.
- reward/body tracking에서 `tcp`를 쓰면 body lookup 실패 위험 있음.
- 현재 command/reward target body는 `palm_link`로 둠.
- 나중에 정확한 tool center point가 필요하면 `palm_link` 기준 offset frame을 별도로 다루는 방향임.

## Task 구현 정리

- `Indy-Wuji-Reach` task 구성함.
- 기존 Neuromeka `Indy-Reach` 스타일을 참고함.
- DirectRLEnv from scratch보다 ManagerBasedRLEnv/MDP 구조를 우선함.
- 기존 `Indy-Reach`를 덮어쓰지 않고 `indy_wuji` 새 task로 분리함.
- `INDY7_WUJI_RIGHT_CFG` asset cfg 사용함.
- `indy_wuji/env_cfg.py`에 task override 둠.
- `indy_wuji/__init__.py`에 gym registration 둠.
- `indy_wuji/learning/rsl_rl_cfg.py`에 RSL-RL config 둠.
- base/teacher/student/CMDP cfg 골자 맞춤.
- active registration은 `Indy-Wuji-Reach` 하나로 둠.
- teacher/student/CMDP registration은 future block으로 둠.

## Action / Observation 정리

- action은 arm 6축만 사용함.
- hand 20축은 아직 policy action에 넣지 않음.
- 처음 목표는 손가락 제어가 아니라 arm end-effector tracking임.
- hand joint는 articulation에는 남아 있음.
- hand joint는 initial arm tracking에서는 observation에서도 제외함.
- arm-only observation으로 낮춤.
- `joint_pos` shape는 18임.
- `joint_vel` shape는 18임.
- `pose_command` shape는 7임.
- `action_history` shape는 12임.
- 전체 policy observation shape는 55임.
- actor/critic input feature는 55임.
- action output은 6임.
- `joint_vel` reward penalty도 arm 6축 기준으로 제한함.

## Reward / Command 정리

- command term은 `ee_pose`임.
- command type은 `UniformPoseCommand`임.
- reward는 position tracking, orientation tracking, end-effector speed, action rate, joint velocity penalty 중심임.
- position reward와 orientation reward는 현재 학습 안정성 확인용 기본값임.
- 현재 목표는 reward 최적화보다 env가 정상 생성되고 학습 loop가 도는지 확인하는 것임.
- 오래 학습하기 전 reward/command range/decimation 튜닝 여지 있음.

## RSL-RL / Script 수정 정리

- `rsl-rl-lib==5.0.1` 기준 호환 문제 확인함.
- `train.py`와 `play.py`에 deprecated cfg migration 대응 필요함.
- `handle_deprecated_rsl_rl_cfg()` 호출을 기준으로 처리함.
- task cfg에서 actor/critic을 수동으로 우회 설정하지 않는 방향으로 정리함.
- `play.py`에서 pretrained checkpoint import 경로 문제 확인함.
- IsaacLab 2.3.2 환경에 맞게 `isaaclab_rl.utils.pretrained_checkpoint` fallback 적용함.
- `play.py`에서 `runner.alg.policy` / `actor_critic` 접근 실패 확인함.
- rsl-rl 5 기준으로 `runner.export_policy_to_jit()` / `runner.export_policy_to_onnx()` 사용함.
- play 실행까지 확인함.

## 실행 확인

- `--num_envs 1 --max_iterations 1` smoke train 통과함.
- `--num_envs 32 --max_iterations 5` headless train 통과함.
- GUI 실행 확인함.
- GUI에서 robot asset 보임.
- GUI에서 Fabric GPU 실행 상태 확인함.
- `Fabric GPU (Properties not updated)` 문구는 GUI property sync 안내 수준으로 판단함.
- 사용자가 `--num_envs 128 --max_iterations 20` 실행함.
- `512/100` 학습 run도 진행해 checkpoint 생성됨.
- 최신 확인 checkpoint는 `logs/rsl_rl/indy_wuji_reach/2026-07-08_18-16-06/model_99.pt`임.
- headless play로 checkpoint load 확인함.
- play에서 actor input 55, action output 6 확인함.
- play loop 완료 확인함.

## 주요 실행 명령

- 1회 smoke train 실행함.

```bash
python scripts/rsl_rl/train.py --task Indy-Wuji-Reach --headless --num_envs 1 --max_iterations 1
```

- 중간 train 실행함.

```bash
python scripts/rsl_rl/train.py --task Indy-Wuji-Reach --headless --num_envs 128 --max_iterations 20
```

- 권장 1시간 run으로 안내함.

```bash
python scripts/rsl_rl/train.py --task Indy-Wuji-Reach --headless --num_envs 512 --max_iterations 100
```

- GUI train 확인함.

```bash
python scripts/rsl_rl/train.py --task Indy-Wuji-Reach --num_envs 1 --max_iterations 1
```

- checkpoint play 확인함.

```bash
python scripts/rsl_rl/play.py --task Indy-Wuji-Reach --num_envs 1 --load_run 2026-07-08_18-16-06
```

## 오늘 확인한 주의사항

- `tcp`를 body tracking target으로 쓰지 않음.
- 현재는 `palm_link` 사용함.
- `SceneEntityCfg`는 여러 manager term에 재사용하지 않음.
- 각 term마다 fresh cfg를 쓰는 방향임.
- IsaacLab core 파일은 되도록 수정하지 않음.
- 기존 example을 덮어쓰지 않고 새 task/env로 구성함.
- `env_isaaclab` 기준으로 실행함.
- 코드 수정 후 작은 smoke test부터 함.
- commit은 사용자가 직접 확인 후 진행함.
- 임의 commit 하지 않음.
- 사용자가 만든 변경은 되돌리지 않음.

## 다음 할 일

- `512/100` run 로그 마지막 구간 확인함.
- mean reward 추세 확인함.
- position error 추세 확인함.
- orientation error 추세 확인함.
- action std 확인함.
- NaN/PhysX error 여부 확인함.
- GUI play에서 움직임 이상 여부 확인함.
- 안정적이면 `512/500` 이상으로 확장 검토함.
- 성능이 애매하면 command range 조정함.
- orientation reward weight 조정 검토함.
- decimation/action scale 조정 검토함.
- TCP가 꼭 필요하면 `palm_link` 기준 offset 처리 검토함.
- hand action은 arm tracking 안정화 후 확장함.
