# 2026-07-14 Activity

- close-start cube grasp 구조를 점검함.
- 큐브가 손 근처에 시작하지 않는다는 사용자 지적을 확인함.
- 원인은 close-start cube의 `x/y`만 손 파지 중심 근처였고 `z`는 바닥 높이였기 때문임.
- 받침면 높이를 `BASE_Z=0.40`으로 정함.
- cube 중심 높이를 `0.43m`로 맞춤.
- `CubeGraspSceneCfg`에 `{ENV_REGEX_NS}/Support` kinematic cuboid를 추가함.
- `cube_lift` reward를 받침면 기준으로 계산하게 `surface_z`를 추가함.
- TensorBoard `cube_clearance` metric도 받침면 기준으로 보정함.
- `hand_floor` penalty도 받침면 기준으로 보정함.
- close-start cube 위치를 `(0.692, -0.369, 0.430)`으로 정정함.
- 이전 `(0.704, -0.279, 0.430)`은 cage 중심에서 y로 약 `9cm` 벗어나 zero action에서 cube가 밀렸음.
- 정정 후 reset `palm_facing=0.987`, zero action 30 step 뒤 `0.997`로 확인함.
- 따라서 현재 close-start에서만 `palm_facing=0`, `joint5` scale 축소가 타당하다고 정리함.
- `Indy-Wuji-Cube-Grasp` smoke test 통과함.
- 확인된 shape는 action `18`, policy observation `57`임.
# 2026-07-14 — cube grasp 포기, 근본 원인 규명

## 결론 먼저

**cube grasp를 접었다.** 그리고 오늘 하루 종일 헤맨 원인이 두 가지로 좁혀졌다.

### ① 정육면체는 "목표 파지"를 정의할 수 없다
논문은 `g = (hp, hr, hj)` — 목표 손 위치/회전/관절각 — 을 알고 있고, `r_hr`이 "이 방향으로
돌려라"를, `r_hj`가 "이 관절각이 진짜 파지다"를 **직접** 보상한다.
**대칭인 정육면체는 목표 회전을 정의할 수 없어서 그걸 전부 못 쓴다.**
그래서 대리 지표(`palm_facing` 축, 게이팅, `closability`)를 발명했고, 전부 뚫렸다.

### ② action space가 논문과 근본적으로 다르다  ★ 이게 더 크다
논문 원문 (Section III-B):
> "An action represents a **relative displacement** in 3D hand position, hand rotation, and hand
>  joint positions. **The arm joint targets are calculated via Inverse Kinematics (IK).**
>  Finally, the joints are controlled with PD controllers."
> "6 DoF UR5e + 11 DoF Schunk SIH hand. The joints of the hand are **coupled, leaving five
>  controllable DoF**. Thus, an action is an **11-element vector**: three for hand position
>  displacement, three for hand rotation (Euler), five for hand joints."
> "v_hp_max = 1 m/s, Δt = 0.0333 s"  -> step당 최대 3.3cm

| | 논문 | 우리 |
|---|---|---|
| action | **상대 변위** (손위치 3 + 회전 3 + 손가락 5 = **11D**) | **절대 관절각** (팔 6 + 손가락 12 = **18D**) |
| 팔 제어 | **IK** | 관절 직접 |
| 크기 제한 | `v_max x Δt = 3.3cm/step` **내재적** | `scale` x `clip_actions` **수동** |
| 제어 주파수 | 30 Hz | 30 Hz (오늘 맞춤) |

**이 차이가 오늘 사고 대부분의 근원이다:**
- **`scale`/`clip_actions` 발산**: 논문 action은 "손을 3cm 옮겨라"라 물리적으로 유계임.
  우리는 "관절을 X rad로 가라"라서 scale이 작으면 정책이 |a|를 키우고, 상한이 없으면 발산함.
- **`arm_manipulability` 페널티가 불필요**: IK가 팔을 풀어주므로 정책이 팔을 접어 특이점에 빠질 수 없음.
  그 페널티를 만든 이유 자체가 "관절을 직접 명령하기 때문"임.
- **`r_hp`/`r_hr`이 action space와 일치**: 정책이 손 pose를 명령하고 보상도 손 pose에 걸림.
  우리는 관절을 명령하면서 손 방향을 보상하려 해서 `palm_facing` 축을 추측하는 삽질을 함.
- **손가락 5 DOF vs 12 DOF**: 탐색 공간이 훨씬 작음.

IsaacLab에 `DifferentialInverseKinematicsActionCfg`가 있고, Lift-Cube에 `ik_abs_env_cfg.py` /
`ik_rel_env_cfg.py` 예제가 있음.

---

## 오늘의 실패 기록 (반복하지 않기 위해)

### 실패 1: `clip_actions` 없음 + `scale` 과소 -> 정책 발산  ★사수님이 잡음
```
|a| 평균 1.5 (범위는 [-1,1]),  |Δa| 최대 9.66
-> 관절 목표가 한 step에 1.93 rad(110°) 점프
-> 팔이 velocity_limit로 왕복,  큐브를 67cm 날림
```
**나는 "손이 87cm 솟구쳤다"는 결과만 보고 "물리 튕김"이라 단정했다.**
사수님이 "action을 찍어봐라"고 해서 잡혔다. **상태(state)가 아니라 제어 입력(action)을 봐야 했다.**
그리고 `Episode_Reward_Raw/action_rate = 167` (= |Δa| ~0.68) 이 이미 TensorBoard에 찍혀 있었는데
그걸 보고도 흘려봤다.

수정: `scale=1.0` + `clip_actions=1.0`.  둘은 **한 쌍**이다:
```
scale 작음 + clip 없음  -> 정책이 |a|를 키워서 보상하려 함 -> 발산
scale 작음 + clip 있음  -> 관절이 조금밖에 못 움직임      -> 도달 불가
scale 큼   + clip 없음  -> 여전히 상한 없음              -> 발산
scale 큼   + clip 있음  -> 정상
```

### 실패 2: `palm_facing` 축을 세 번 틀림
1. `(1,0,0)`이 hold와 상관계수 +0.003 (무상관) -> "틀린 축"이라 결론
2. `(0.19,0.28,0.94)`로 바꿈
3. **사용자가 "그게 진짜 손바닥 법선 맞아?"라고 물어서** 직접 기하 측정 ->
   **`(1,0,0)`이 손바닥 법선이 맞았다.** 내 진단이 틀렸다.
4. 그런데 더 나아가보니 **`hold` 자체가 87% 퇴화**였다:
   무작위 자세 81,920개 중 hold>0.1인 48개를 실제로 오므려보니 **87%가 오므리면 hold가 내려감**
   (= 손끝이 물체에서 멀어짐). 손가락 쫙 편 채 선분만 물체를 관통시킨 퇴화 자세들.
5. **썩은 지표(hold)와의 상관으로 축을 고르니 퇴화를 최적화하는 축을 고른 것이었다.**

### 실패 3: 게이팅 발명 -> telescoping 파괴 (왕복 farming)
```python
reward = reward * gate      # gate in [0,1].  음수(후퇴)에도 곱함
```
```
겨누고 다가감    +0.5 x 1.0 = +0.5
안 겨누고 물러섬 -0.5 x 0.0 =  0     <- 페널티 소멸!
-> "겨눔 -> 접근 -> 방향 버림 -> 공짜 후퇴" 왕복으로 reach를 무한 수확
```
**보상은 오르는데 지표는 나빠졌다** (mean_reward 2.86인데 cage_sdf가 0.129 -> 0.413 역주행).
수정: `torch.where(reward > 0, reward * gate, reward)` — 양수에만 게이트.
**차분형에 곱셈 게이트를 걸 땐 반드시 부호를 나눌 것.**

### 실패 4: "리셋 자세가 이미 물체를 잡고 있어야 한다"고 착각  ★사용자가 잡음
논문 close-start: "The arm is set to a **neutral configuration** with a high manipulability score."
**팔은 중립 자세이고 파지는 정책이 학습한다.** 나는 손이 이미 큐브를 감싼 채로 시작하게 만들려고
테이블을 넣고 손목을 뒤집고 CEM으로 파지 자세를 찾으려 했다. 전부 헛수고.
사용자: "팔을 강제로 맞추는 게 아니라 학습으로 맞춰 가야하는 거 아님?"

---

## 관통하는 패턴 (이게 진짜 교훈)

**나는 매번 "대리 지표"를 만들고, 그걸 검증하지 않고 결정했다.**
```
palm_facing 축   <- hold와의 상관으로 고름.  hold가 87% 퇴화라 무의미했음
게이팅           <- 발명.  telescoping을 깨뜨림
closability      <- 발명.  안 쓰기로 함
"파지 중심"       <- 손끝 3개의 중심.  엄지가 마주보지 않으면 무의미
손목 자세         <- "파지중심이 palm 아래" + manip.  둘 다 대리 지표
```

**속일 수 없는 신호는 하나뿐이다: `cube_clearance` (물체가 실제로 떴는가).**
`hold`, `cage_inside`, `opposition`, `palm_facing` 전부 정책에게 뚫렸다.
**그리고 이 프로젝트에서 `cube_lift`는 한 번도 0을 벗어난 적이 없다.**

### 규칙
1. **새 보상/지표를 만들면, 최종 목표와의 상관계수부터 재라.** 5분이면 된다.
2. **"그 상태가 존재하는가"를 샘플링으로 먼저 확인하라.**
   (`hold>0.1 AND palm_facing>0.7`은 61,440개 중 **0개**였다. 존재하지 않는 걸 학습시키고 있었다.)
3. **"보상은 오르는데 지표는 나빠진다"가 보이면 무조건 허점이 있다.**
4. **상태(state)가 아니라 제어 입력(action)을 봐라.**
5. **정책이 지표를 거스르면, 정책이 아니라 지표를 의심하라.**

---

## 코드 상태 (되돌림 완료)

### 남긴 것
```
clip_actions = 1.0,  scale = 1.0        (rsl_rl_cfg.py:21, env_cfg.py:76)
sim.dt = 1/60,  decimation = 2 (30 Hz)  (cube_grasp_env_cfg.py:145-146)
play.py --print_action                   (진단 도구)
사용자/codex 작업 전부 (support 받침면, joint5 scale 0.05로 진동 해결 등)
```

### 뺀 것 (오늘 내가 넣었다가 실패한 것)
```
finger_closability                       (함수 삭제)
reach의 facing 게이팅                     (palm_cfg / gate_floor 파라미터 포함)
palm_normal_b (0.19,0.28,0.94) -> (1,0,0)  (rewards.py + managers.py)
가중치 reach 10 / palm 8 / action_rate -0.005
     -> reach 0.3 / palm 1.0 / action_rate -0.0003
Indy-Wuji-Cube-Grasp task
테이블 (넣었다 뺌)
```

---

## 다음 (functional_grasp)

**골격은 이미 있음:**
```
functional_grasp/
├── chopsticks_grasp_env_cfg.py     # 직육면체 (0.03 x 0.03 x 0.16) = 젓가락 프록시
├── cfg_skeleton.py                 # 전체 흐름 + 설계 결정
└── mdp/
    ├── target_grasp.py             # g = (hp, hr, hj)
    └── rewards.py                  # r_hp, r_hr, r_hj, r_orient, r_T
```

**반드시 바꿔야 할 것 (오늘 배운 것):**
```
1. action space를 논문처럼:  IK(팔) + 관절(손가락),  상대 변위
   -> IsaacLab의 DifferentialInverseKinematicsActionCfg
   -> scale/clip 참사, arm_manipulability 페널티가 전부 불필요해짐
2. 목표 파지 g = (hp, hr, hj) 정의  -> r_hr이 방향을 직접 보상 -> palm_facing/게이팅 불필요
3. 손가락 DOF 축소 검토 (논문 5, 우리 12)
4. 가중치는 논문 그대로 (r_reach 1 / r_hold 25 / r_orient 500 / r_T 5000)
5. 커리큘럼 (close-start에서 r_man 끔, 물체를 손 5cm 앞, 팔은 manip 높은 중립)
```

---

## 정정: cube grasp는 유지하고 grasp+lift baseline으로 되돌림

- `Indy-Wuji-Cube-Grasp` task는 제거하지 않고 유지함.
- 별도 Stage1/Hard/Easy를 나누지 않고 단일 task로 유지함.
- active action은 arm 6축 + thumb/index/middle 12축 = 18D임.
- active observation은 54D임.
- active reward는 `finger_cage_reach=3`, `finger_cage_hold=5`, `cube_lift=50`, `cube_support=2`, `palm_facing=0`, `arm_manipulability=0`, `hand_floor=0.2`, `action_rate=-0.0003`임.
- `cube_support`는 큐브를 받침 아래로 누르는 실패 모드를 잡기 위해 추가함.
- scripted lift probe 결과, 손을 닫으면 hold는 오르지만 cube clearance는 0 근처이고 lift reward는 0임.
- arm 단일축 ±1 후보와 강한 손/가벼운 큐브 probe에서도 실제 lift는 발생하지 않음.
- 따라서 긴 학습 전에 실제로 들어올릴 수 있는 arm/hand sequence 또는 초기 자세를 먼저 찾아야 함.

## 오후: 파지 능력 검증 (grip capacity)

- 목적: "이 손이 0.30kg 큐브를 물리적으로 들 수 있는가" — 정책/보상 배제, 힘부터 확인
- **결론: 힘은 충분하다 (단, 파지 방식은 별개)**
  - 오므림 40~50%에서 4/4 성공 (큐브 물고 팔로 +28cm 상승, palm 9cm 유지)
  - 32 env 중 23 성공. cube_lift=0은 하드웨어가 아니라 정책/보상 문제로 확정
  - ※ 정정 (저녁, GUI 확인): 4/4는 새끼/손바닥 받침 hold. 엄지·중지 미접촉 → 집게는 미검증
- 손 기하 실측 (`scripts/debug/hand_geometry.py`)
  - 엄지-중지 간격: 17.6cm(폄) → 5.8cm(30%) → 2.8cm(40%, 최소) → 다시 벌어짐
  - 물리는 창은 30~50% 하나뿐. 60% 이상 조이면 수박씨처럼 짜냄 (1/4)
  - 검지-중지 항상 1.5~4.2cm → 사실상 엄지 vs (검지+중지) 2점 집게
- 측정 버그 3개 수정 (모두 가짜 "파지 불가" 판정 원인)
  1. 매 step 순간이동 고정 → 관통 폭발 → 중력 차단 + 속도 0으로 교체
  2. tip_link 원점 = 관절 위치(패드 아님) → 큐브가 접힌 약지/새끼 위에 얹혔음 (GUI로 발견)
  3. joint1 감소 = 손 하강 (상승 아님) → 판정 기준 "놓은 뒤 상승 > 5cm"로 교체
- 도구: `scripts/debug/grip_capacity.py` (`--gui`로 눈 확인, 기본 오므림 0.40)
- 교훈: 숫자가 세 번 속였고 GUI 스크린샷 한 장이 원인을 밝힘. 접촉 문제는 눈으로 봐야 함

## 저녁: 시작 자세 변경 + 편 손가락 + 재학습 준비

- 시작 자세를 joint3/4=-1.61/-1.62로 변경함 (탐색 최상위, 위에서 덮치는 방향).
- 전 손가락을 폄(0.0)으로 변경함. 접힘(1.2)은 자가 충돌로 폐기함 (사용자 결정).
- 손가락 액추에이터를 제조사 값(kp 2/2/1/1, kd 0.05)으로 교체함. effort 0.6 유지함.
- 리셋 시 액션 밖 관절의 위치 목표가 0으로 남는 버그를 `hold_joints_at_default` 이벤트로 고침.
- grip_capacity.py에 pinch 판정(엄지·중지 접촉 거리)을 추가함.
- 새 자세 + 편 손가락 집게 검증은 0/32임. 오므림 중 큐브가 24.4cm 밀려남. 집게는 미입증 상태임.
- 재학습 실험(사용자): 질량 0.10, episode 6s, hold 25 / lift 50 / reach 8 / palm 4 / floor 1.0.

## 밤: cage 가상점을 손끝 쪽으로 이동

- 내부 등분점 [0.25,0.5,0.75]는 헐렁한 새장에서도 hold 고점이 나와 lift gate가 헛것에 열렸음.
- `cage_points`에 `point_fractions` 파라미터를 추가함 (기본 None = 기존 동작).
- reach/hold/lift 셋 다 (0.1, 0.5, 0.9)로 통일함. 손끝이 표면에 닿아야 고점이 나옴.
- hold/lift `depth_max`를 0.02 → 0.005로 줄임. "닿으면 만족"으로 포화시켜 수박씨 짜냄을 막음.
- 다음 재학습부터 적용됨. hold 절대값이 낮아지는 것은 기준 강화로 정상임.
