# ACTIVITY 2026-07-18

## 비교 라운드 판정 (0번 관문 종료)

- **큐브 질량: 0.1kg 승.** run `2026-07-17_23-06-15` — success 98.2% (iter 6,866), 낙하 1.3%,
  error_pos 0.028. 동일 iter의 0.2kg 대조군(88.0%) 대비 +10%p, 첫 이륙 4배 빠름
  (82% @1,715 vs 대조군 첫 success ~3,000). 곡선 98% 고원 → 세워도 됨.
  주의: 0.1kg은 커리큘럼 초반값 — 실물 이관 단계에서 질량 복귀 계획 필요.
- **박스 lift: A′(lift 0) 기각.** run `2026-07-17_23-15-16`, iter 6,427 대조 —
  success 0% 전 구간 (대조군 35.8%).
  - 들기·운반은 됨: max clearance 0.200(목표 높이), transport 수입 동급(3.35 vs 3.57).
  - 실패 지점 = 정착: φ 현금화 후 내려놓고 hold 파밍 (hold 수입 2배, time_out 83%).
    goal 체류가 테이블 체류와 수입이 같은데 힘만 듦 → 15스텝 유지 사건이 안 생겨
    r_T 발견 실패. 예상 문제 1+2 혼합형 → 판정 트리대로 B안 처방.
  - 파지 자세 가설(이동 인센티브 → 자세 붕괴)은 기각: A′도 max 맞물림 0.995,
    cage 감싸기 0.873으로 대조군보다 깊음. per-step 평균의 구성 편향에 주의
    (지표 함정 상세는 AGENTS.md 1-a 판정 절).

## B안(goal_proximity 연금) 배선 구현

- `rewards.py`의 미배선 부품 `object_goal_proximity`를 `box_mdp_cfg.py`에
  `goal_proximity` 항으로 배선 (기본 weight 0 — 파일 기본값 = 검증된 A안 승자 구성).
- 스모크 통과: 4 env × 2 iter, reward 테이블에 cube_lift 0 / cube_transport 0 /
  goal_proximity 75 확인, env.yaml 스냅샷 정상. 스모크 런 폴더는 삭제함.
- 함정 발견: hydra 오버라이드에 `weight=0`(정수)을 쓰면 타입 에러 —
  `0.0` 소수점 필수 (CLAUDE.md 함정 목록에 추가).

## 다음 라운드 (2슬롯 병행)

- **슬롯 A — B안 fresh** (정착 계곡의 설계된 처방 검증):

```bash
cd ~/wuji_indy_lab_51/nrmk_isaaclab_wuji && conda activate env_isaaclab
python scripts/rsl_rl/train.py --task Indy-Wuji-Box-Transport --headless \
  env.rewards.cube_lift.weight=0.0 \
  env.rewards.cube_transport.weight=0.0 \
  env.rewards.goal_proximity.weight=75.0
```

- **슬롯 B — 승자 구성 + 유지력 조이기** (lift 50 기본값 + hold_steps 30):

```bash
python scripts/rsl_rl/train.py --task Indy-Wuji-Box-Transport --headless \
  env.terminations.success.params.hold_steps=30
```

- 판정 계획: 슬롯 A는 "success 이륙 여부 + 정착(종료 시 clearance 0.15+)"이 핵심,
  대조군은 lift50 run `2026-07-16_16-33-21` 곡선. 슬롯 B는 success 하락폭 −10%p 이내면
  통과 → 다음 fresh에서 gate_threshold 0.3→0.4 또는 hold_steps 60.
- 두 런 판정 후 승자 구성이 로드맵 1번(랜덤화 축 확장)으로 진출.

## 우선순위 변경 (사용자): B안 보류 → orientation 우선

- B안 통합 실험은 나중으로 미룸. 큐브 쪽 goal_proximity 배선은 철회(revert), 박스 쪽은
  weight 0 부품으로 잔류. 앞선 "다음 라운드(슬롯 A=B안)" 계획은 이 결정으로 대체됨.
- 새 우선순위: ① orientation goal 맞추기 ② 직육면체 크기 경우의 수 확대.

## 논문의 orientation 방식 확인 (thesis.md 기반)

- Pre-grasp 논문(2307.16752): r_orient(500)는 임의 goal이 아니라 "잡기 좋은 명목 자세로
  굴리기" 차분형. 성공은 손↔물체 상대 자세(target grasp) 기준.
- TriFinger: 6-DoF goal pose는 quaternion error를 직접 안 쓰고 8-keypoint 거리 +
  bounded logistic kernel. 비교 실험에서 pos+quat 분리 보상은 orientation 학습이 느림.
- 번역: v1 판정은 geodesic 15°로 출발하되, shaping 필요 시 keypoint 방식으로.
- 구조 차이 정리: 우리 성공은 물체↔월드(운반), 논문은 손↔물체(기능 파지), 파지 방식은
  우리가 자유(cage) — cage 파지는 물체가 안에서 헛돌 수 있어 ori 제어가 약할 수 있음
  → v2(opposition 조건), v3(target grasp g, 젓가락 1개 프록시) 단계 계획 수립 (AGENTS 참조).

## Orientation v1 구현 (완료, 스모크 통과)

- `rewards.py`: `square_prism_ori_error()` — 정사각 단면 프리즘 대칭 8개(y축 90°계 × 뒤집기)
  중 최소 geodesic 각. 단독 수학 검증: 대칭들 0° / y+15°→15° / 세움→90° / z+170°→10°.
- `ObjectAtGoalHeld`에 `ori_limit` 파라미터 추가 (None 기본 = 기존 동작, 큐브 태스크 불변).
- `box_mdp_cfg.py` success에 `ori_limit=0.2618`(15°) 기본 탑재 — 박스 기본 설정 = v1.
  끄기: `env.terminations.success.params.ori_limit=null`.
- `managers.py`에 `box_ori_error` 지표 추가 (cube/final/min/max 4종 자동 방출, 스모크 확인).
- obs/action 불변. 종료 조건 변경이므로 다음 박스 런은 fresh.
- v1 런치 (오버라이드 불필요 — 파일 기본값이 v1):

```bash
python scripts/rsl_rl/train.py --task Indy-Wuji-Box-Transport --headless
```

- 판정 계획: 대조군 = lift50 런(`2026-07-16_16-33-21(success)`, ori 조건 없는 43.5%+).
  v1 success가 그 대비 얼마나 깎이는지 + `cube_final/box_ori_error`가 15° 아래로 정착하는지.
  미이륙 시 처방 순서: keypoint shaping → v2(opposition 조건).

## 크기 랜덤화 확장 구현 (슬롯 B, 사용자 결정값)

- 결정: 단면 하한 1.5×1.5cm, 길이 최대 20cm (1.5×1.5×20 = 젓가락 프록시 포함).
- 구현: `randomize_box_dims`에 `length_range` 추가 — 길이를 폭과 **독립** 샘플
  U(1.5×폭, 20cm). ratio 방식으로는 얇은 상자가 항상 짧아져서 젓가락형이 안 나옴.
  box_mdp_cfg 기본값: width (0.015, 0.06) + length_range (0.0, 0.20).
- 검증: 샘플 10만 개 — 폭 1.5~6.0cm, 길이 2.3~20.0cm, "길이>1.5×폭" 위반 0건,
  젓가락형(폭<2cm & 길이>15cm) 비율 3.3%. 슬롯 B 구성(ori null) 4env 스모크 통과.
- ⚠ 단면 2.8cm 미만은 커플링 손끝 간격 실측 밖 미검증 구간 (grip_capacity 사전 실측은
  생략하고 진행 — 사용자 결정). 크기-버킷 분해로 물리/학습 한계 판독 예정.

## 슬롯 현황 (2026-07-18 저녁)

- **슬롯 A (도는 중)**: orientation v1 — ori 15° + **구 크기(3~6cm)**. 사용자가 기본값으로
  런치했고 크기 확장 코드 반영 전 스냅샷이라 크기는 이전 분포임 (단일 변수 유지, 의도 부합).
- **슬롯 B (런치 대기)**: 크기 확장 + ori 없음:

```bash
python scripts/rsl_rl/train.py --task Indy-Wuji-Box-Transport --headless \
  env.terminations.success.params.ori_limit=null
```

- ⚠ 파일 기본값은 이제 "ori 15° + 새 크기" 결합임. 슬롯 A(구 크기 ori) 재현이 필요하면
  `env.events.randomize_box.params.width_range=[0.03,0.06] env.events.randomize_box.params.length_range=null`
  오버라이드 필요.

## 사고: 슬롯 A 런 크래시 (에이전트 실수, 재시작 필요)

- 원인: 에이전트가 스모크 잔여물 정리용 `rm -rf`를 날짜 글롭으로 돌려 사용자의 라이브 런
  폴더(`2026-07-18_14-30-22`)까지 삭제 → TB writer FileNotFoundError로 ~5분 만에 크래시.
- 손실: iteration 0~1 (~5분). 재발 방지 규칙은 CLAUDE.md 함정 목록에 추가됨.
- 슬롯 A 재시작은 크기 확장이 파일 기본값이 된 이후라 **구 크기 오버라이드 필요**:

```bash
python scripts/rsl_rl/train.py --task Indy-Wuji-Box-Transport --headless \
  "env.events.randomize_box.params.width_range=[0.03,0.06]" \
  env.events.randomize_box.params.length_range=null
```

  (ori 15°는 파일 기본값이라 그대로 켜짐. length_range=null이면 기존 ratio 1.5~3 경로.)
