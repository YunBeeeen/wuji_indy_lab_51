# 2026-07-12 Activity Log

- 이 문서는 2026-07-12 작업 내용과 판단을 날짜별로 기록한 활동 일지 문서임.

## 전체 요약

- 2026-07-11의 cage reward가 학습에 성공했으나, 나온 자세가 기괴했음. 손바닥이 하늘을 향하고 검지와 중지가 교차함.
- 원인은 cage가 "엄지 ↔ 중지" 관계만 보고 검지와 손바닥을 전혀 제약하지 않았기 때문임.
- 가상점을 6개에서 12개로 확장해 검지를 cage에 포함시킴.
- 손바닥 방향 reward(`palm_facing`)를 검토했으나 **자의적**이라는 결론으로 철회함.
- 대신 `cube_lift`를 도입함. **"들지 못하는 자세는 파지가 아니다"**가 자세를 선별하는 non-arbitrary 기준임.
- reward는 4개가 되었고 action shape `18`, observation shape `57`은 불변임.

---

## 1. 문제 → 원인 → 수정 (전체 체인)

### 문제 1 (2026-07-11에 해결): 큐브에 도달하는데 쥐지 않음

- 원인: reward가 "손끝 -> 큐브 **중심**" 거리였음.
- 큐브 중심은 표면에서 `0.03 m` 안쪽이라 손끝이 **물리적으로 도달할 수 없는 목표**임.
- gradient가 항상 큐브 속을 향하고, 최댓값이 도달 불가 지점에 있음.
- `body_weights=(3,1,1)`로 엄지가 가중평균의 `60%`를 차지함.
- 따라서 가장 싼 해법이 **"엄지 하나만 큐브에 박고 나머지는 방치"**가 됨.
- 수정: 목표를 큐브 **표면(SDF)**으로 바꾸고, 손끝 개별 점이 아니라 **파지 간극**을 대상으로 하는 cage reward 2종 도입함.

### 문제 2 (오늘): cage는 성공했으나 자세가 기괴함

- run `2026-07-11_19-50-45` (1583 iter) 수렴 결과:
- `thumb_middle_opposition` `-0.965` -> `+0.922`. 엄지와 중지가 큐브 양쪽에 위치함.
- `cage_inside_frac` `0.000` -> `0.837`. 가상점 6개 중 5개가 큐브 내부임.
- `cage_span` `0.157` -> `0.111`. 손을 오므림.
- 지표상으로는 완벽한 파지였음.
- 그러나 GUI 확인 결과 **검지와 중지가 교차했고 손바닥이 하늘을 향함**.
- `enabled_self_collisions=True`이므로 물리 설정 문제가 아님.
- 원인: cage가 "엄지끝 ↔ 중지" 선분만 봄. **검지는 reward에 등장조차 하지 않음.** 손바닥 방향도 제약 없음.
- 즉 "엄지와 중지가 큐브를 양쪽에서 물기만 하면" 나머지는 무엇을 하든 만점이었음.
- 정책은 reward를 정직하게 최대화했고, 지표도 정직하게 만족됨.
- 수정: 가상점 6개 -> 12개. 검지를 cage에 추가함.

### 문제 3 (오늘): 감쌌는데 들지 못함

- `cube_lift` final `0.002 m`. 사실상 `0`임.
- 원인: **드는 것을 보상하는 항이 하나도 없음.**
- 수정: `cube_lift` (cage-gated, 조밀형) 추가함.

---

## 2. 논문에는 왜 이 문제가 없었는가

- 논문 reward는 `r(t) = r_grasp + r_man + r_MP + r_T`임.
- `r_grasp = r_hp + r_hr + lambda * r_hj`.
- `r_hp`: 손의 **위치**를 목표 파지 자세로.
- `r_hr`: 손의 **회전**을 목표 파지 자세로. **손바닥 방향을 잡음.**
- `r_hj`: **손가락 관절각**을 목표 파지 자세로. **손가락 꼬임을 막음.**
- 우리가 구현한 것은 `r_man`의 `r_reach` + `r_hold`뿐임.
- 논문이 "엄지-중지 선분 하나(6점)로 충분하다"고 한 것은 **`r_grasp`가 손 자세를 붙잡은 상태**에서의 관찰임.
- 큐브는 대칭이라 목표 파지 자세가 없어 `r_grasp`를 구현할 수 없었고, 그 결과가 기괴한 자세임.

---

## 3. 수정 1: 가상점 6 -> 12 (검지 추가)

- `CAGE_BODIES`를 `[thumb_tip, *opposing]` 5개로 확장함.
- `finger1_tip_link` (엄지끝, 모든 선분의 기준점).
- `finger2_tip_link`, `finger2_link3` (검지 끝, 검지 중간마디). **신규**.
- `finger3_tip_link`, `finger3_link3` (중지 끝, 중지 중간마디). 기존.
- 대향 body 4개 x 등간격 3점 = **12점**.
- 논문이 명시적으로 제시한 확장임 ("it is straightforward to utilize several finger pairs at the same time").
- **엄지+검지+중지는 최종 목표인 젓가락 그립과 동일하므로 임시방편이 아님.**

### 코드

- `_cage_points` -> `cage_points`로 일반화함. 정확히 3개 body 요구 -> `[thumb, *opposing]` N개 허용.
- reward 수식(`object_in_finger_cage`, `ObjectCageProgressReward`)은 **변경 없음.** 점 개수만 늘고 평균을 냄.
- `managers.py`의 `_cage_body_names`를 동일하게 5개로 맞춤. reward와 metric이 같은 점을 보게 함.
- metric 신규 2종: `thumb_index_opposition`, `cage_span_index`.
- 검지 opposition을 따로 로깅해야 함. **중지만 보면 검지가 교차해도 알 수 없기 때문임.** 이것이 2026-07-11 실패를 놓친 이유임.

### 의도된 부작용

- 12점 평균이므로 검지가 큐브에 닿지 못하면 `finger_cage_hold` 값이 통째로 낮아짐.
- 기존 "엄지-중지만" 자세는 점수가 약 절반으로 떨어짐. **더 이상 충분하지 않게 만드는 것이 목적임.**

---

## 4. 수정 2: `palm_facing` 검토 후 철회

### 제안했던 것

- "손바닥이 물체를 향하도록" 하는 reward.
- 손바닥 법선 축을 실측함. `palm_link` 로컬 **`+x`**임 (손가락이 오므라들 때 손끝이 이동하는 방향, 성분 `+0.92`).
- 특정 회전을 지정하지 않고 한 축만 정렬하므로 roll은 자유임.

### 왜 철회했는가

- **논문이 `r_hr` (목표 손 회전)을 주는 이유는 "기능(functional grasp)" 때문임.**
- 드릴은 검지가 트리거에, 스프레이는 분사 버튼에 닿아야 함. **"당길 수 있게 쥐어라"**는 요구임.
- 드릴을 그냥 집어 올리기만 할 거라면 `g_r`이 필요 없음.
- **큐브에는 기능 요구가 없으므로 목표 회전이 필요 없는 것이 맞음.**
- 사람이 손바닥 방향을 정해주면 그것이 자의적 제약이 되고, 젓가락에서는 어차피 `r_hr`로 교체되며 버려짐.
- **목표 회전은 "잡기"에서 나오는 것이 아니라 "쓰기"에서 나옴.**

### 중요한 관찰 (사용자 지적)

- **`opposition`은 손바닥 방향을 보지 못함.**
- "엄지와 손가락이 큐브 양쪽에 있는가"만 보므로, `+0.5`를 넘어도 손바닥이 하늘을 볼 수 있음.
- 따라서 자세 판정을 opposition만으로 하면 안 됨.

---

## 5. 수정 3: `cube_lift` — 진짜 선별 기준

### 원리

- **들지 못하는 자세는 파지가 아님.**
- 하중을 견디는지만 물으면 자세를 지정할 필요가 없음. **물리가 자세를 결정함.**
- 논문도 같은 이유로 `r_lift`를 추가함. "satisfy the constraint without actually stably grasping the object" (**fake success**) 방지용임.
- 2026-07-11 run이 정확히 그 상태였음. `opposition +0.92`, `inside_frac 0.84`인데 `cube_lift 0.002 m`임.
- **손바닥이 하늘을 보든 무엇을 하든, 큐브를 안정적으로 들 수 있으면 그것은 유효한 파지임.** 우리가 반대할 이유가 없음.
- 우리가 원하는 것은 "예쁜 자세"가 아니라 **"작동하는 파지"**임.

### 구현: `object_lift_in_cage` (`mdp/rewards.py`)

- 기존 `object_lift_bounded`를 교체함.
- `r = cage_gate * clamp(height / lift_height, 0, 1)`.
- `cage_gate`는 `object_in_finger_cage`의 값임 (`0..1`). **감싸지 않으면 lift 보상이 `0`임.**
- gate가 없으면 **"파지 없이 큐브를 튕겨 올리는" 편법**이 가능함.
- **조밀형(dense)임.** 현재 정책이 만드는 `2 mm` 상승도 gradient를 가짐.
- 희소형이었다면 영원히 `0`이라 학습이 시작조차 안 됨.
- `initial_height=0.03` (큐브 spawn 높이). reset이 x/y만 랜덤화하므로 유효함.
- `lift_height=0.08`. 이만큼 뜨면 만점임.

---

## 6. 역할 분담 (중요: cage는 자세를 유도하지 않음)

- 2026-07-11에 "cage가 손바닥 방향까지 잡아준다"고 판단했으나 **틀렸음.** 6점 cage가 손바닥 하늘로 수렴한 것이 반증임.

```
finger_cage_reach  ->  "파지 간극을 물체 위로"   (물체가 손가락 사이에 오는가)
finger_cage_hold   ->  "오므려라"                 (손가락 사이 공간이 물체를 파고드는가)
cube_lift          ->  "들어라"                   ★ 자세를 결정하는 것은 이것
```

- **어느 항도 "손바닥이 어디를 봐야 한다"고 말하지 않음. 그리고 그것이 의도임.**
- cage는 "물체가 손가락 사이에 있는가"만 봄. 손바닥 방향은 자유임.
- 자세를 선별하는 것은 `cube_lift`, 정확히는 **물리**임. 하중을 못 견디는 자세는 보상을 못 받아 도태됨.

---

## 7. 최종 Reward 구성

- `finger_cage_reach`: `weight=0.3`. 논문 Eq.14. 가상점 12개 -> 큐브 표면 SDF의 차분.
- `finger_cage_hold`: `weight=1.0`. 논문 Eq.15. 가상점이 큐브 내부로 파고든 깊이.
- `cube_lift`: `weight=3.0`. 논문 `r_lift`. `cage_gate * 큐브 높이`.
- `action_rate`: `weight=-0.0003`.
- 가중치 순서는 논문의 `r_T >> r_orient >> r_hold >> r_reach`를 따름.
- action scale `0.2`. action shape `18`, observation shape `57` 불변.
- `py_compile` 통과, smoke test 통과, 12점 생성 검증함.

---

## 8. 학습 판정 지표

- `Metrics/cube_final/cube_lift`: `0.002`에서 올라가는가. **이것이 전부임.**
- `Metrics/cube_final/thumb_index_opposition`: 검지도 큐브를 무는가.
- `Metrics/cube_final/thumb_middle_opposition`: 중지도 큐브를 무는가.
- `Metrics/cube_final/cage_inside_frac`, `cage_span`, `cage_span_index`.
- **`--resume` 금지.** reward가 바뀌어 value function이 무의미함.

---

## 9. 실패 시 용의자 순서

### 1순위: 손가락 actuator가 물다

- `stiffness=8.0`, `damping=0.5`.
- 약지/새끼 떨림을 줄이려고 낮춰놓은 값임.
- `0.08 kg` 큐브를 마찰로 들려면 손끝 수직력이 필요한데, 너무 물러서 못 쥘 수 있음.
- 그러면 아무리 학습해도 **물리적 상한**에 막힘.
- 검증법: 손가락을 강제로 오므린 채 팔을 올려 큐브가 따라오는지 봄.

### 2순위: 바닥

- 큐브가 맨바닥(ground plane) 위에 있음. 테이블 없음 (`reach_env_cfg.py:47` 주석 처리됨).
- 팔이 `z=0.03`까지 굽혀 내려가야 해서 manipulability가 나쁘고 손목이 손을 원하는 자세로 못 돌림.
- 실측: `palm_link` `z=0.082`, 검지끝 `z=0.030`, 중지끝 `z=0.018`.
- 논문이 `r_MP` (manipulability penalty)를 넣고 "neutral configuration with high manipulability"를 명시한 것이 이 문제임.

### 3순위: 제어 주파수

- physics `1/60 s` x `decimation 24` = environment step `0.4 s`. 제어 `2.5 Hz`임.
- 손가락 폐합에는 매우 거침. 논문은 `30 Hz`임.

---

## 10. 젓가락 로드맵 (무엇이 넘어가고 무엇이 추가되는가)

### 왜 젓가락에는 orientation이 필요한가

- 젓가락은 **기능 요구가 있음**.
- 윗젓가락은 엄지/검지/중지로 움직여야 함. 아랫젓가락은 엄지 뿌리와 약지 사이에 고정됨. 두 젓가락은 앞을 향해 나란해야 함.
- 이것을 못 만족하면 젓가락을 "잡았어도" 쓸 수 없음.
- **따라서 손 회전이 물리적으로 결정됨. 자의적이지 않음.**
- 반면 큐브는 아무렇게나 집어도 되므로 손 회전이 결정되지 않음.
- **기준은 "물체가 대칭이냐"가 아니라 "기능 요구가 있느냐"임.**

### 그때 쓸 논문 항 (constraint-based, 논문이 권하는 쪽)

```
g = [ g_ifp^O ,  g_r^O ]          (논문 Eq. 19)
       |            +-- 손 회전 목표            -> r_hr 로 유도
       +-- 검지 끝의 3D 목표 위치 (젓가락 위)   -> r_hp 로 유도
```

- 손가락 관절각(`g_j`)은 주지 않음. "검지 끝이 여기, 손이 이 방향" 두 개만 주고 나머지는 정책이 찾음.
- 논문이 이쪽을 선호함. "easier than specifying a full explicit grasp configuration".

### 넘어가는 것 / 추가되는 것

- **cage 기계 전체** (가상점, SDF, reach/hold): 그대로 씀 (`r_man`).
- **가상점에 검지 포함**: 그대로 씀. 젓가락 그립이 엄지+검지+중지이기 때문임.
- **`cube_lift`**: `r_lift`로 그대로 씀. 논문도 fake success 방지용으로 씀.
- **`r_hr` (손 회전)**: 젓가락에서 **추가**. 큐브에서는 자의적이라 안 넣음.
- **`r_hp` (검지 끝 목표 위치)**: 젓가락에서 **추가**.
- `palm_facing`: 만들지 않았음. 만들었다면 `r_hr`로 교체되며 버려졌을 것임.
- **버려지는 것이 없음.** 큐브에서 안 넣은 것들이 젓가락에서 추가될 뿐임.

### 전체 진행 순서

```
1. 큐브 파지          <- 지금        "간극으로 물고 오므려서 든다"     (r_man + lift)
2. 직육면체 + 랜덤 방향               "물체 기하에 적응한다"
3. 젓가락 pre-grasp                   "기능적 자세로 잡는다"          (+ r_grasp = 논문 완성)
4. 젓가락 조작        <- 논문 밖      "잡은 채로 벌리고 오므린다"
```

- 2번: 직육면체의 긴 축은 손의 파지 간극보다 길어 물 수 없으므로, **cage가 짧은 축을 자동으로 찾게 됨.** orientation 항 없이 물체 기하가 스스로 제약함.
- 4번은 논문 범위 밖임. `thesis.md`의 SimToolReal (tool-use trajectory)이나 TriFinger (잡은 물체를 목표 자세로) 참고가 필요해질 것임.

---

## 11. 운용 규칙 (추가)

- **`opposition`만으로 자세를 판정하지 말 것.** 손바닥 방향을 보지 못함.
- **검지 opposition을 따로 볼 것.** 중지만 보면 검지 교차를 놓침.
- **`managers.py`의 `_cage_body_names`는 `CAGE_BODIES`와 반드시 동일해야 함.** 한쪽만 바꾸면 metric이 reward와 다른 점을 측정함.
- **"손끝 -> 물체 중심" 거리 reward를 절대 다시 넣지 말 것.** 도달 불가능한 목표이고, 엄지 가중치와 결합하면 "엄지만 박기"가 최적해가 됨.
- **정책이 이상하게 행동하면 정책이 틀린 것이 아니라 reward가 틀린 것임.** 지금까지의 모든 문제가 그러했음.
