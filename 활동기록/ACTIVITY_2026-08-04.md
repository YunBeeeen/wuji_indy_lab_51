# 2026-08-04 활동 기록

## 빈손 뒤집기 리워드 해킹 수정 (palm_up/stick_in_palm에 lift_gate 복원)

- 증상(사용자): "잡질 못하네". overnight run `23-52-23`(7329 iter, 08-03 23:52 시작,
  success 0.6 + palm_up cage-decouple 적용본)이 **파지를 아예 포기**.
- 지표 판독: `palm_up_cos` best **0.98**(팜 완벽히 위) 인데 `cage_inside` 0, `palm_distance` min
  **0.31m**(손이 스틱서 31cm), `cube_lift` 0, `stick_in_palm`/`in_palm_success` 0. mean_reward는
  it833에 ~51로 정체 → **it833에 "빈손으로 팜만 뒤집기" 국소최적을 찾고 고정.**
- 원인(내 버그): 08-03에 `palm_up`을 cage에서 떼면서 `progress×goal_gate`만 남김. "goal_gate가
  빈손 뒤집기를 막는다"는 판단이 **틀림** — 목표(BASE_Z+0.15)가 스틱 테이블 높이(~0.2535)와 가까워
  **테이블에서도 goal_gate가 0이 아니라 ~0.033 잔여**. palm_up weight 20000이 그 잔여를 증폭해
  파지 없이도 palm_up 보상 6.4 → 파지보다 빈손 뒤집기가 이득.
- 수정: `palm_up`·`stick_in_palm`에 **`lift_gate` 복원**(cage는 계속 뺌).
  - `PalmUpProgressReward`: `progress×goal_gate` → **`progress×lift_gate×goal_gate`**.
  - `StickInPalmProgressReward`: `progress×goal_gate×palm_up_factor` →
    **`progress×lift_gate×goal_gate×palm_up_factor`**.
  - `_lift_gate`는 clearance<2cm(테이블)에서 **정확히 0** → "스틱이 들려야" 뒤집기·담기 보상 켜짐 →
    빈손 해킹 차단. cage는 계속 빼둬(핀치 풀려도 flip 안 죽게) 08-03 취지 유지.
- `py_compile` PASS. 보상 gate 변경이라 **fresh run 필수**, 현재 23-52-23는 중단 대상.

### 교훈
- 큰 weight 항에서 gate를 뺄 땐 "대체 gate가 hack 상태에서 정확히 0인지" 확인할 것. `goal_gate`는
  물체 정지 위치에서 잔여값이 남아 부족했고, `lift_gate`(clearance)는 테이블에서 정확히 0이라 확실.
