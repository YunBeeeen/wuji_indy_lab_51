# 2026-08-23 활동 기록

## 목표

- 다양한 checkpoint를 안전하게 교체하는 배포 계약 검사.
- Hand-real2 750 checkpoint 배포.

## 변경

- `verify_policy_contract.py` 신설.
- Observation dim, term order, normalization, action scale, pregrasp 대조.
- Run의 `params/env.yaml`에서 pregrasp를 읽는 loader 추가.
- Deploy rig의 일부 오차를 hard reject에서 경고로 조정.

## 결과

- ONNX 105D/20D shape만으로는 충분하지 않음을 확인.
- Hand-real2 750 checkpoint의 계약과 reset pose 확인.
- NEUTRAL `[0,0]`도 학습된 command 의미를 가질 수 있음을 정리.

## 판단

- Checkpoint와 함께 env.yaml·agent.yaml 보관.
- Pregrasp, joint limit, scale, mode semantics를 네트워크 외부 계약으로 관리.

## 다음

- 실물 전류·온도와 장시간 실행 안전성 검증.
