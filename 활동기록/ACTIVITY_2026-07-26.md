# Activity 2026-07-26

## penta(5손가락) wrap 부트스트랩 실패 → quad(4손가락)로 축소

**관찰 (box 21-44-48, penta wrap + 고정 palm-up + keypoint)**:
- penta 게이트(`Episode_Reward_Raw/finger_cage_hold`, func=balanced_penta_cage_gate)가
  iter 300에서 0.002 최고 → iter 700에 0.0005로 **하락**. 처음부터 안 열린 채 노이즈 수준.
- 손끝: thumb 0.073→0.042(접근), ring 0.077→0.069(느린 접근), **little 0.088→0.095(멀어짐)**.
- cube_lift raw 0.00003 ≈ 0 (penta 게이트 닫혀 lift 보상 안 나옴). mean_clr 0.02(바닥).

**원인 (구조적, 기다려도 안 풀림)**:
1. 새끼(little)는 엄지 대향이 **해부학적으로 제일 어려움**(손바닥 반대편) → 안 붙어 min을 0으로 끔.
2. **닭-달걀**: lift(강한 보상)가 penta 뒤에 갇힘 → 5손가락 wrap을 당길 강한 신호가 없음
   (grip progress ~0.3점뿐). penta hold 연금도 게이트 닫히면 0.
3. **물리**: 물체가 바닥(mean_clr 0.02)이라 5손가락 wrap하려면 반대쪽 손가락이 바닥에 부딪힘.
   좀 들려야 감싸는데, 들려면 게이트가 열려야 함.
- 참고: box 과제 자체는 wrap 불요 — 09-43-43이 tripod로 이미 성공. wrap은 Phase 1 파지 목표라
  과제상 할 일 없는 약지·새끼를 인위적으로 강제하는 것 → 약한 보상으론 안 됨.

**대응 (사용자 판단: "새끼 포기, 약지까진 된다")**: penta → **quad(엄지+검지+중지+약지 4손가락)**.
- 새끼는 **게이트에서 제거**, `pinky_grip`(약한 progress)로만 "오면 좋고" 유도.
- 근거: 약지는 접근 중(6.9cm), 새끼만 min을 끌던 것 → 새끼 빼면 약지 붙으며 게이트 열릴 여지.

**코드 변경**:
- `functional_grasp/mdp/rewards.py`: `_min_cage_gate`(엄지 대 N손가락 min 공용 헬퍼) +
  `balanced_quad_cage_gate`(검지·중지·약지 3쌍) + `object_lift_in_balanced_quad_cage` 추가.
  penta·tripod 함수는 보존(되돌리기).
- `grasp/box_mdp_cfg.py`: `finger_cage_hold`·`cube_lift`를 penta → quad (pinky_cage_cfg 제거).
  `pinky_grip` term·`BOX_PINKY_CAGE_BODIES`·`_PINKY_GRIP_REGION`은 유지. 각 지점 되돌리기 주석.
- py_compile OK. fresh 00-04-17이 quad로 시작(iter 35).

**판별 (iter 1000~2000)**: `finger_cage_hold`(quad 게이트)가 penta처럼 하락 vs 상승 반전.
상승하면 4손가락 wrap 검증. cube_lift raw가 0에서 뜨는지 같이 볼 것.

## Box keypoint/success 파지 gate tripod → quad 정합

**변경 전 기준선**:
- run `indy_wuji_box_transport/2026-07-26_15-37-18`.
- `finger_cage_hold`·`cube_lift`는 엄지 대 검지·중지·약지의 quad gate였지만,
  active `keypoint_goal`·`transport_success`는 엄지 대 검지·중지만 보는 tripod gate였음.
- 변경 전 소스 전체를
  `grasp/box_mdp_cfg.py.keypoint_tripod_backup_2026-07-26`에 보존함
  (백업 직후 active 파일과 SHA-256
  `083001b0ec13698116c310690ab6976c9ab1ee0a3d7086405e7d51ade182a767` 일치).

**변경**:
- `KeypointMaxGoalProgressReward`, `KeypointMaxAtGoalHeld`,
  `KeypointGoalReachedBonus`의 gate를 `balanced_tripod_cage_gate`에서
  `balanced_quad_cage_gate`로 교체하고 `ring_cage_cfg`를 추가함.
- active `keypoint_goal`과 `transport_success`에
  `BOX_RING_CAGE_BODIES`를 배선함. 물체의 8-corner keypoint 정의는 변경하지 않음.
- 실행 중인 `15-37-18`에는 영향 없음. 다음 Box run은 fresh로 비교함.
- `python -m py_compile`과 `git diff --check` 통과.
