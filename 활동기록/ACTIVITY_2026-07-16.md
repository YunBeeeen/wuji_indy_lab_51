
## 오후: Box-Transport 태스크 구현

- 큐브 태스크를 동결하고 별도 태스크 Indy-Wuji-Box-Transport로 복제 구현함.
- env마다 다른 비율보존 직육면체(단면 3~6cm × 비율 1.5~3)를 prestartup 스케일로 만듦.
- 관측을 64로 확장함 (크기 3 + quat 4). 공유 헬퍼는 하위호환 방식으로만 일반화함.
- 스모크(4 env)와 end-to-end 프로브 통과함 (버퍼=USD=물리 3자 일치, 정착 잔차 0mm).
- (후속) lift 단계 구성으로 잠금: transport 3종 세트를 주석처리함. 관측 64와 goal 커맨드는
  유지해 재활성 시 체크포인트가 이어짐. 스모크 통과함.
- Box lift-only 첫 런을 시작함 (4096 env, 스냅샷 e5d6a68).
- 큐브 태스크 보상 v2: lift 100→50, transport를 역수 포텐셜+창(±10cm)으로 전환함
  (φ=0.05/(0.05+min(d,0.1)), weight 4000). 스모크는 box 학습 종료 후 필수임.

## 오후 2: 큐브 보상 v2.1 (창 제거)

- transport 창(±10cm)을 제거하고 전 구간 역수 φ로 확정함 (사용자 수정).
  근거: 스폰 랜덤으로 "높이 정답 + 횡 창 밖" 에피소드가 무신호였음 — 역수 φ가
  "원거리 완만 + 근거리 가파름" 2층을 연속으로 내장하므로 창이 불필요함.
- 최종 사다리: lift(0~8cm) → φ(연속) → r_T(goal ±5cm 유지 → +500·종료).
- 큐브 태스크 스모크는 box 학습 종료 후 필수임 (시그니처 변경).
- box transport 3종을 재활성함 (관문: lift 이륙 + 고정 불안정 관찰). palm_facing은
  best-so-far(+전용)로 전환함. 재시작 전 스모크 필수임.
- 재학습 원칙 확정 (사수님): 보상/태스크 변경 시 fresh, resume 금지. box/큐브 다음 런 모두
  fresh로 함. drop_penalty 커리큘럼은 단일 런 내 curriculum manager 방식으로 재설계 예정임.
