# 2026-08-02 활동 기록

## palm-in 보상을 cage에서 분리 (손바닥에 담기 goal)

- 배경: chopsticks flip 런(`07-58-22` 반성공)이 cos_up~0.92에서 정체하고
  `in_palm_success`가 전 구간 0. 원인은 `stick_in_palm`·`in_palm_success`가
  tripod cage로 gate돼, 손을 뒤집어 핀치가 풀리는 순간(=손바닥에 담기는 순간)
  보상이 0이 되고 success의 `gate>0.3`을 얇은 스틱(cage 최대 ~0.1)이 못 넘김.
- 사용자 방침: "빠르게 돌려서 손바닥에 닿을 정도"(날리진 않게). #1만 적용,
  in_palm_success는 gate 자체 제거, throw_penalty(#3)는 안 함.

### 코드 변경
- `nrmk_isaaclab_wuji/.../functional_grasp/chopstick_mdp_cfg.py`
  - `StickInPalmProgressReward.__call__`: 반환 `progress*gate*lift_gate` →
    `progress*lift_gate*palm_up_factor`. `palm_up_factor = clamp(cos_up, 0, 1)`.
    cage(`balanced_tripod_cage_gate`) 계산 삭제. `disp_local, _` →
    `disp_local, palm_quat`로 받아 `_palm_up_cos(palm_quat)` 계산.
  - `StickInPalmSuccessBonus.__call__`: `valid` 조건에서 `& (gate > gate_threshold)`
    삭제 + cage 계산 삭제 → `(distance<goal_radius) & (cos_up>palm_up_min) &
    (clearance>clearance_threshold)`.
  - 두 함수의 cage 파라미터(`index_cage_cfg`, `middle_cage_cfg`)·`gate_threshold`는
    RewTerm cfg 호환 위해 시그니처에만 존치(미사용). cfg는 손대지 않음.
- `nrmk_isaaclab_wuji/isaac_neuromeka/env/managers.py`
  - `_compute_cube_distance_metrics`에 **`palm_up_cos`** metric 신설: 팜 로컬 +x
    (=팜 평면 법선, 실측 0.965,-0.008,0.262)의 world z성분. 보상 `_palm_up_cos`와 동일 축.
    `_cube_metric_names`에 등록 → `Metrics/cube{,_final,_min,_max}/palm_up_cos` 로깅.

### 검증
- `python -m py_compile`로 두 파일 문법 확인(PASS). sim 미기동
  (`hand_setting` train.py 가동 중이라 probe/GUI 금지 규칙 준수).
- 보상 의미 변경이라 **fresh run 필수**. hand_setting 런 종료 후 실행 예정.

### 관찰 지표 (다음 런에서 볼 것)
- `Metrics/cube*/palm_up_cos` — flip 진행(신설). 1로 향하면 팜업 완성.
- `Episode_Reward_Raw/in_palm_success` — 성공 빈도(전엔 구조적 0이었음, >0 나와야 함).
- `Metrics/cube*/cube_lift`, `Episode_Reward/cube_lift` — lift 유지되는지.
- `Metrics/cube_final/cage_inside_frac` — 붕괴(뒤집을 때 이탈) 여전한지.
- `Metrics/cube*/palm_facing` — 파지 정렬(flip 아님, 혼동 주의).

## in_palm_success에 월드 위치 조건 추가 + 목표 높이 조정

- 배경: cage 분리 후 팜업은 되나(run 14-27-10) 팔을 접어 **테이블 근처 저공에서 팜업(그릇)**.
  원인: palm_up이 위치 무관 + palm-in 항이 lift_gate(5cm)로만 gate → 5cm면 자격 충족.
- 방침(사수님): 팜업은 어디서 하든 무관, **마지막에 목표 위치로 수렴**하게. 위치를 높은 점으로
  못박으면 접힌 자세로는 못 닿아 자격 상실 → 자연스러운 편 자세 유도(순서 강제 아님, 최종상태 규정).
- 코드 변경
  - `chopstick_mdp_cfg.py` `StickInPalmSuccessBonus`: 시그니처에 `command_name="cube_goal"`,
    `goal_radius_w=0.06` 추가. 본문에 `goal_pos_w = env_origins + command[:,:3]`,
    `dist_to_goal = ‖stick_root − goal_pos_w‖` 계산 후 `valid`에 `& (dist_to_goal < goal_radius_w)` 추가.
  - 같은 파일 `in_palm_success` RewTerm params에 `command_name`·`goal_radius_w` 명시.
  - `chopsticks_grasp_env_cfg.py`: `cube_goal.ranges.pos_z` `BASE_Z+0.20` → **`BASE_Z+0.15`**(적당한 높이).
- palm_up / stick_in_palm 보상은 위치 무관 유지(lift_gate 5cm만) — 사용자 요구대로.
- `python -m py_compile` PASS. 보상/성공 조건 변경이라 **fresh run 필수**.
