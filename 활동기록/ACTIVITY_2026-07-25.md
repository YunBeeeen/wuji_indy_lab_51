# Activity 2026-07-25

## A/B 판독 (chopstick 쿼터니안 vs box keypoint) — 같은 랜덤 pose 조건

> 두 런 모두 roll/yaw (1.57, 3.14) + resample (5,10) + 동일 grip으로 확인(env.yaml 대조).
> chopstick `08-47-53`(쿼터니안 분리), box `00-54-01`(keypoint).

- **box(keypoint)**: ori min 15°(0.264) / mean 53°(0.93), 계속 하강. lift·파지 더 좋음(max_clr 30cm).
- **chopstick(쿼터니안, 옛 grip 런 00-49-29)**: ori min 34° / mean 87° 벽. 못 뚫음.
- **결론**: 랜덤 pose에선 keypoint 우세. pos·ori 융합 거리(d)라 위치가 멀어도 자세 신호가 안 끊김.
  쿼터니안 분리형은 `pos<5cm`에서만 자세가 켜져 신호 단절. → **자세 목표항은 keypoint 채택.**
- chopstick keypoint 이식 시 주의: box 8-대칭(tip↔tail 동일) 대신 젓가락 4-대칭(`_STICK_TIP_SYMS`,
  tip 보존)을 keypoint 코너 매칭에 써야 함(tail-forward 성공 인정 방지).

## chopstick grip 교정 (08-47-53) — 창발 파지에 region 정렬

- play 관찰: 정책이 실제로 만든 파지가 **엄지 −x / 중지 +x**(설정과 반대)였음.
- grip region을 그 창발 파지에 맞춤(엄지 −x/중지 +x/검지 +z). 동시에 box와 동일해져 A/B가 깨끗해짐.
- 효과: 새 런이 **iter 200에 엄지·검지 표면 접촉(0.9cm)+cage 0.32** 형성. 옛 런은 iter 4000에야
  도달했던 상태 → 파지가 ~3800 iter 빨라짐. lift는 아직(마지막 단계, iter 464 기준 정상).

## 파지 보상 예산 진단 — 왜 자세가 안 다듬어지나

- grip 항(FingertipGripProgressReward, progress형) 예산 ~1.3점 vs cube_lift(gate형) 240~800점.
  **grip 완성 보상이 lift의 ~1/500** → 자세는 `lift×gate` 결합으로만 형성. 느슨-lift 국소최적 위험.
- 개입 기준: 느슨-lift가 **드롭(stick_dropped) 유발할 때만** `gate^k`로 sharp하게.

## Phase 1 재정의 + 주먹(wrap) 파지 구조 적용 (코드 수정)

**설계**: Phase 1 종료 = 주먹(5-finger wrap) 파지 → transport → palm-up. wrap이 Phase 1 목표 자체.
엄지 −x면, **나머지 4손가락(검지·중지·약지·새끼) 모두 +x면**을 감쌈. 긴 스틱 중력 토크를 접촉
분산(길이축 stagger)으로 버팀. lift/hold는 penta 게이트가 5손가락 wrap을 강제.

**변경 파일·내용**:
- `functional_grasp/mdp/rewards.py`: `balanced_penta_cage_gate`(엄지 대 4손가락 4쌍 cage의 min),
  `object_lift_in_balanced_penta_cage`(gate^gate_exponent × clearance, exponent 기본 1.0) **신설**.
  tripod 함수는 보존(되돌리기).
- `functional_grasp/chopstick_mdp_cfg.py`:
  - `CHOPSTICK_RING_CAGE_BODIES`(엄지+finger4_tip+finger4_link3), `CHOPSTICK_PINKY_CAGE_BODIES`
    (엄지+finger5) 추가. `RING_CFG`/`PINKY_CFG` 추가.
  - regions: **INDEX surface_axis 2(+z)→0(+x)** (엄지 반대면 합류). RING/PINKY region 추가(+x).
    axial stagger: INDEX(−0.65,−0.35) / MIDDLE(−0.55,−0.25) / RING(−0.45,−0.15) / PINKY(−0.35,−0.05).
    THUMB(−x) 유지.
  - `ring_grip`/`pinky_grip` RewTerm 추가(weight 40, progress형).
  - `finger_cage_hold`: `balanced_tripod_cage_gate` → `balanced_penta_cage_gate`.
  - `cube_lift`: `object_lift_in_balanced_tripod_cage` → `object_lift_in_balanced_penta_cage`
    (+ring/pinky cage, `gate_exponent=1.0` 노브).
- **백업**: `chopstick_mdp_cfg.py.tripod_backup_2026-07-25` (검증된 09-42-28 tripod 원본). 각 변경
  지점에 되돌리기 주석 있음.
- 검증: py_compile OK, finger4/5 body 이름 URDF·env_cfg_common에 실재 확인. **Isaac Sim 스모크는
  현재 도는 학습 종료 후**(obs/action dim + env 생성 + penta 게이트 값 확인).

**성공-래치 커리큘럼 수식**(Phase 전환, study.md에 상세): `L←max(L,s1)`,
`R=(1−L)·R_p1+L·R_p2`. 연속 함수라 fresh-run 규칙 준수 + critic 충격 최소.

## 정정 + 방향 전환 (2026-07-25 저녁) — wrap을 chopstick→box(keypoint)로 이동

**정정(앞 절 "A/B 결론" 폐기)**:
- chopstick 쿼터니안 "34° 자세 벽"은 방법 문제가 아니라 **grip 오설정 아티팩트**. 08-47-53(00-49-29
  대비 유일 차이 = 엄지·중지 x부호 스왑)에서 ori min 4°까지 내려감. env.yaml 대조로 확인.
- box keypoint "정체"도 방법 아님. **09-43-43(고정 pose)은 keypoint로 err_pos 4.3cm·ori min 3°·
  success 상승**으로 잘 됐음. 00-54-01 정체는 **랜덤 pose(1.57~3.14)** 탓.
- "kp_raw 0.0003=신호 죽음" 오판 철회 — 잘 된 09-43-43도 동일 raw(텔레스코핑이라 정상).
- box 정체 상세: 높이는 goal(0.20)에 맞추나(mean_clr 0.18) **수평 위치·자세가 안 좁혀짐**
  (err_pos 0.11→0.16 후퇴, ori min 15° 정체). keypoint weight 150000(예산~45) vs cube_lift
  150(예산~360)로 lift 예산이 8:1 우세지만, 같은 weight로 고정 pose는 됐으므로 weight가 근본은 아님.

**핵심 판단**: 랜덤 자세매칭은 두 Phase 어디도 요구 안 함(Phase1 palm-up 고정, Phase2 위치만).
→ 실제 과제는 keypoint가 강한 고정-pose 영역. **keypoint(box)를 주력으로, 파지를 주먹(wrap)으로.**

**코드 변경(방향 전환 반영)**:
- **chopstick wrap 철회·복원**: `chopstick_mdp_cfg.py`를 `*.tripod_backup_2026-07-25`로 되돌림
  (08-47-53이 돌고 있는 known-good tripod, 엄지−x/중지+x/검지+z). wrap 마커 0 확인.
- **box에 주먹(wrap) 적용**: `grasp/box_mdp_cfg.py`에 BOX_RING/PINKY_CAGE, _RING/_PINKY_CFG,
  _RING/_PINKY_GRIP_REGION(+x), ring_grip/pinky_grip term 추가. _INDEX +z→+x. finger_cage_hold·
  cube_lift를 `balanced_penta_cage_gate`/`object_lift_in_balanced_penta_cage`(gate_exponent=1.0)로.
  백업 `box_mdp_cfg.py.keypoint_backup_2026-07-25`.
- `functional_grasp/mdp/rewards.py`의 penta 함수는 유지(box가 씀).
- py_compile 양쪽 OK. **box는 스모크(obs/action dim + penta 게이트) 후 fresh.**
- ⚠ box는 랜덤 pose(1.57~3.14) 상태 그대로임 — 실제 요구(고정 palm-up)로 갈지 별도 결정 필요.

**결정(같은 날 후속)**: box goal을 **고정 palm-up 자세로 확정** — 주먹 파지 + keypoint가 위치·자세
둘 다 매칭. cube_goal roll(0)/pitch(0)/yaw(0.785398) 고정, resample 끔(1e9). 09-43-43 검증 방식.
랜덤 pose(1.57~3.14) 철회. ⚠ yaw 0.785=palm-up 여부는 학습된 grasp 측정으로 확정(현재 근사).
