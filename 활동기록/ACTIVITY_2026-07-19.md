# ACTIVITY 2026-07-19



## 7월 18일자 학습 run

 cd ~/wuji_indy_lab_51/nrmk_isaaclab_wuji
  conda activate env_isaaclab

  python scripts/rsl_rl/play.py \
    --task Indy-Wuji-Box-Transport \
    --num_envs 1 \
    --checkpoint "/home/lsc/wuji_indy_lab_51/nrmk_isaaclab_wuji/logs/rsl_rl/indy_wuji_box_transport/2026-07-18_20-56-53_longstick/model_7600.pt" \
    "env.observations.policy.box_ori_to_target=null" \
    "env.events.randomize_box.params.width_range=[0.015,0.06]" \
    "env.events.randomize_box.params.length_range=[0.0,0.20]" \
    env.terminations.success.params.ori_limit=null \
    env.terminations.success.params.hold_steps=1000000
    
## v1.1(keypoint) 판정: 기각 — 매달림은 보상이 아니라 파지 기하 문제

- run `2026-07-18_22-48-57` (keypoint φ + ori 15°), iter 6,607: success 0, 자세 오차
  1.2 rad(70°) 횡보, 낙하 4.6%(안정), error_pos 0.119 (12cm 접근).
- 결정적 단서: ori 조건·보상이 다른 슬롯 B도 종료 자세 1.16 rad로 동일 → 기울임은 정책
  선택이 아니라 역학 (케이지 파지의 중력 토크 매달림). play 관찰(사용자): 손가락 2개
  불안정 파지 = 2점 접촉 경첩.
- 인과 사슬 완성: v1 "유인이 없다" → v1.1 "유인은 있는데 수단이 없다" → v2 = 파지 기하.
- depth_max 증량 검토 후 기각 (수박씨 배출 + 점접촉 마찰은 토크에 무력).

## 감싸쥐기 cage 부품 준비 (미배선)

- `BOX_CAGE_BODIES_WRAP` (box_mdp_cfg.py): 폭(약지/새끼 tip+link3) + 깊이(검지/중지
  link2) 확장, 11 bodies. 활성화 = `BOX_CAGE_BODIES = BOX_CAGE_BODIES_WRAP` 한 줄.
- 활성 시 gate 값 하락(점 평균 희석) → gate_threshold 0.3 재보정 필요 주석 명기.
- 링크 존재 확인: indy7_wuji_right.urdf에 finger*_link1/2/3 + tip_link.

## 스틱 라운드 계획 (사용자 확정)

- 순서: ① 스틱 기준선 (단면 1.5~3cm × 길이 18~20cm, 현행 손끝 cage, ori 없음 — CLI
  오버라이드 런치) → ② cage 개편(WRAP) fresh 비교 → ③ 개편 승리 시 단면 1cm 확장.
- 1cm는 실측 밖(손끝 간격 2.8cm 하한)이라 감싸쥐기 준비 후 도전.

## 감싸쥐기(WRAP) 활성화 + 새 라운드 구성 (사용자)

- `BOX_CAGE_BODIES = BOX_CAGE_BODIES_WRAP` 활성 (11 bodies — 폭: 약지/새끼, 깊이: link2).
  모든 gate/hold/reach/success에 일괄 적용. 스모크 통과.
- 라운드 구성:
  - **슬롯 A**: 기존 크기(3~6cm × ratio 1.5~3, 파일 기본) + WRAP + ori 15° + keypoint φ
    — "감싸쥐기가 매달림을 고쳐 ori success를 여는가". 대조군 = v1.1(22-48-57, 손끝 cage).
  - **슬롯 B**: 얇은 스틱(1.5~3cm × 18~20cm) + WRAP + ori 없음
    — "감싸쥐기로 얇은 것을 잡을 수 있는가". 1cm 확장은 이 결과 후.
- 관찰 포인트: gate 값 희석(5→11 bodies) → Episode_Reward_Raw/finger_cage_hold 레벨이
  이전 런 대비 낮게 시작하는 게 정상. success 발화가 지나치게 안 되면 gate_threshold
  0.3 하향 검토. 짧은 물체 부분 감점은 슬롯 A(4.5cm~)에서 유의.

## WRAP 라운드 첫 판독 (iter ~1,150) + 스틱 파지 실패 원인 실측

- 런 정체 (스냅샷 검증): `12-45-27` = 슬롯 A (WRAP + ori 15° + 기존 3~6cm),
  `12-48-25` = 슬롯 B (WRAP + ori 없음 + 스틱 1.5~3cm). 둘 다 WRAP 확인됨.
- **슬롯 A 청신호**: box_ori_error 0.94→0.73 첫 하락 (v1.1은 1.2 횡보) — 감싸쥐기가
  매달림을 잡기 시작. max clearance 0.26, 계속 관찰.
- **슬롯 B 파지 실패 원인 = hand_floor 충돌 (실측)**: 스틱 파지 지점(상판 위 0.75~1.5cm)이
  hand_floor 페널티 존(2cm) 안 → "잡으러 가라"(hold) vs "가지 마라"(hand_floor) 충돌.
  증거: hand_floor raw 악화(−0.77→−0.82), 중지 접근 정체(0.14), cage_sdf_min 관통 0(+),
  clearance 0.005 (들기 전무), displacement 0.096 (밀고만 다님).

## 코드 수정 (재학습은 사용자, 코드만 반영)

- `isaac_neuromeka/tasks/manipulation/grasp/box_mdp_cfg.py` — `hand_floor.params.clearance`
  0.02 → **0.01**: 스틱 파지 높이(≥0.75cm)를 합법화. 근거 주석 동봉.
- `isaac_neuromeka/mdp/commands.py` — `UniformCubeGoalCommandCfg.goal_marker_cfg`:
  구슬(Sphere r=0.02) → **반투명 직육면체** (0.025×0.10×0.025, opacity 0.35, y로 길쭉).
  goal "자세"(월드 정렬)가 보이도록 — 마커 자세 identity = 목표 자세 그대로.
  v2(goal 자세 랜덤화) 시 `_debug_vis_callback`에 orientations 전달 확장 주석 포함.
  ※ 이 마커는 큐브 태스크에도 공유됨 (시각 전용이라 무해).
- 4 env 스모크 통과 (마커 생성 포함), 스모크 폴더 정확 삭제.
- 슬롯 B 재런치 명령 (hand_floor 0.01은 파일 기본이라 기존 인자 그대로):

```bash
python scripts/rsl_rl/train.py --task Indy-Wuji-Box-Transport --headless \
  "env.events.randomize_box.params.width_range=[0.015,0.03]" \
  "env.events.randomize_box.params.length_range=[0.18,0.20]" \
  env.terminations.success.params.ori_limit=null
```

## 슬롯 B 재런치 확인 (스냅샷 검증)

- 새 run `2026-07-19_15-42-29`: clearance 0.01 + ori null + 폭 1.5~3cm ×
  길이 **15~20cm** (사용자가 하한 18→15cm로 확장) + WRAP — 전부 정상.
- 현 라운드: 슬롯 A `12-45-27` (WRAP + ori 15° + 기존 크기) ∥
  슬롯 B `15-42-29` (WRAP + 스틱 + clearance 0.01, ori 없음).
- 판독 순서(B): hand_floor raw 안정 → cage_sdf_min 음수 전환(포획) → clearance 이륙.

## 스틱 런 2차 판독 + hand_floor 감량 (사용자 적용)

- 2차 판독 (`15-42-29`, iter ~1,178, clearance 0.01 효과): hand_floor −0.82→−0.57 완화,
  cage_sdf_min **첫 음수 전환**(−0.0016, 포획 시작). 그러나 새 병목 실측 —
  **엄지 단독 걸치기 파밍**: 엄지만 접근(0.028), 검지/중지/약지/새끼는 이탈 증가(0.14~0.18),
  엄지 근처 가상점만 관통해 hold 평균의 ~1/3(cage_inside_frac 0.38 상한) 부분 점수 수확.
  마주잡기 없음 → 들기 0. 남은 억압 = hand_floor 잔여 벌점(−0.57).
- 코드 수정 (사용자): `box_mdp_cfg.py` `hand_floor.weight` **1.0 → 0.2** (검증 전례값,
  clearance 0.01 유지) — 손가락들이 스틱 옆으로 내려가는 비용 제거. 같은 인자로 재런치.
- 다음 처방 대기: 엄지 파밍 재현 시 hold에 맞물림 팩터(반대편 접촉 없으면 반감) 구조 수정.
- 재런치 확인: 새 run `2026-07-19_18-50-35` (clearance 0.01 + weight 0.2 + 스틱 1.5~3cm ×
  15~20cm + ori null) — 스냅샷 검증 완료.

## 코드 수정: goal 마커 이중화 (위치+자세 동시 표시)

- `isaac_neuromeka/mdp/commands.py` — `goal_marker_cfg`를 마커 2종으로:
  `position_sphere`(r=0.012, 초록) = goal 중심 + `goal_box`(2.5×10×2.5cm, opacity 0.35,
  identity 자세 = 목표 자세) = 맞춰야 할 방향. `_debug_vis_callback`이 같은 지점에
  두 마커를 겹쳐 그리도록 marker_indices 배치 (앞 N=구슬, 뒤 N=박스).
- v2(goal 자세 랜덤화) 확장 지점 주석: 뒤 N개에 command quat 전달.
- 4 env 스모크 통과, 스모크 폴더 정확 삭제. play에서 확인 가능.

## play 진단 (사용자) — 액션 포화 + 말단 토크 한계 실측

- applied ±1 포화: 정책 raw가 ±7~10 (가우시안 무제한 출력 + 크기 비용 부재) → env 클립.
  bang-bang은 기존 관찰의 재확인 (큐브 98%도 이 상태로 달성 — 치명 아님, 진동원).
- **말단 관절 B-판정**: finger2_joint4 target +1.0 vs actual −0.27, err 1.3, 토크 100%(0.6
  포화) — 굽히라는 명령을 받고 풀토크를 쓰는데 못 굽음. "손끝 안 굽힘"의 원인은 유인(A)이
  아니라 액추에이션(B). 처방 후보: 말단 effort 0.6 상향 / kp 2 상향 (사수님 확인 필요할 수
  있음), 보조로 action_l2 소량.
- ⚠ 단 이 진단은 크기 오버라이드 누락 play(기본값 뚱뚱 상자)에서 관찰됨 — 스틱 환경
  재확인 필요 (play에 학습 오버라이드 복붙, CLAUDE.md 함정 추가).

## 스틱 play 재확인 (크기 인자 포함) — "누르기 파밍" 실측 + 노선 결정

- 스틱 환경 play 진단: clearance 음수(테이블에 누름), hold 0.37~0.45 부분점수 수확,
  span 0.12(안 오므림), 중지 말단 joint2/4 명령 +1 vs 실제 −0.3~0 (토크 100% 포화 — 스틱/
  테이블에 막힘), raw ±10~20 (클립 84~100%). "평면 위 얇은 물체"는 표면 추출이라는 별개
  난제 + 누르기 파밍 균형으로 확정.
- **노선 결정 (사용자)**: 스틱 보류, 슬롯 A(두꺼운 박스 ori) 완성 우선 (Path B).
  스틱 복귀 조건 카드: 받침 스폰 커리큘럼 + 맞물림 팩터(누르기 파밍 차단) + 말단 effort 상향.
- keypoint 방식의 랜덤 포즈 호환 확인 (사용자 질문): q_goal이 식의 파라미터라 v2에서
  command quat만 끼우면 됨 — 방식 자체가 TriFinger의 랜덤 6-DoF pose 용도로 설계된 것.

## 코드 수정: 슬롯 A "ori 완성 패키지" 구현 (⚠ 스모크 미실시 — 런치 전 필수)

- `isaac_neuromeka/mdp/curriculums.py` **신규**: `anneal_success_ori_limit` —
  common_step_counter 스케줄로 success의 ori_limit을 45°→30°→15° 조임 (termination
  param을 직접 갱신하는 modify_reward_weight 패턴). mdp/__init__에 재수출 추가.
- `isaac_neuromeka/mdp/observations.py`: `object_ori_error_nearest_sym` **신규** —
  최근접 대칭 목표까지 상대 회전 axis-angle (N,3). "크게 되돌리기" play 실측의 처방.
- `isaac_neuromeka/tasks/manipulation/grasp/box_mdp_cfg.py`:
  - `cube_lift.params.lift_height` 0.08 → **0.20** (호버 급여 제거 — transport 정체 실측 근거)
  - success `ori_limit` 0.2618 → **0.785(45°) 시작값** (커리큘럼이 조임; 커리큘럼 끄면 45° 유지 주의)
  - obs `box_ori_to_target` 추가 → **policy 64 → 67 (fresh 필수, 구 체크포인트 play 호환 깨짐)**
  - `BoxTransportCurriculumCfg` 신규 (ori_limit_anneal, 스케줄 (0,45°)(48k,30°)(96k,15°) —
    48k 스텝 ≈ iter 2,000). TB 확인: Curriculum/ori_limit_anneal.
- `box_transport_env_cfg.py`: curriculum = BoxTransportCurriculumCfg() 배선.
- 상태: py_compile 통과. **4 env 스모크 아직 안 돌림** — 런치 전 스모크 필수
  (obs dim 변경 + 커리큘럼 첫 사용이라 env 생성 검증 필요).
