# 2026-07-30 Activity

## hand_grasp 수렴 후 6-contact 붕괴 진단

- 분석 run: `logs/rsl_rl/hand_grasp/2026-07-29_21-05-32`
- 약 1500 iter:
  - final functional contact count `5.969/6`
  - final full-contact `0.969`
  - mean reward 약 `1227`
- 약 3100 iter:
  - contact count `2.936/6`
  - full-contact `0`
  - mean reward 약 `145`
- 마지막 6145 iter:
  - contact count 약 `2.99/6`
  - full-contact와 success `0`
  - mean reward 약 `57`
- 검지 tip–Stick1, 중지 tip–Stick1, 약지 tip–Stick2가 주로 사라졌고,
  gap·pivot·Stick2 pose reward도 함께 감소함. 따라서 다른 reward를 더 먹기 위해
  3-contact를 선택한 해가 아니라 수렴 뒤 policy가 전체적으로 붕괴한 것으로 판정함.

## 6-contact 보존 reward/termination

- 변경 전 backup:
  `nrmk_isaaclab_wuji/backups/hand_grasp_pre_contact_gate_2026-07-30/`
- 각 semantic contact force를
  `c_i = clip(F_i / 0.02, 0, 1)`로 정의함.
- contact recovery:
  - `functional_contact_mean`: weight `10`
  - `functional_contact_min`: weight `40`
- OPEN/CLOSE gap과 lateral shaping은 `min_i(c_i)`를 곱해 6-contact가 약해지면
  함께 사라지도록 함.
- joint reference, Stick1 pivot, Stick2 pose는 파지를 놓친 상태에서 돌아올 수 있도록
  낮은 가중치의 ungated recovery 신호로 유지함.
- contact-collapse termination:
  - acquire: 6/6 force `>=0.02 N`을 5 policy step
  - release contact 판정: force `>=0.01 N`
  - latch 뒤 유지 접촉 `<=4/6`이 10 step 지속되면 종료
  - 5/6은 종료하지 않고 contact reward로 복구
- success weight `30000`, PPO entropy coefficient `0.001`, action `20D`,
  policy observation `103D`는 그대로 둠.
- smoke:
  `logs/rsl_rl/hand_grasp/2026-07-30_09-32-09`
  에서 reward 11개와 termination 5개가 resolve되고 24 policy step을 통과함.

## Gap과 lateral 학습 신호 분리

- 변경 전:
  `contact_gate * exp(-abs(gap-target)/0.005 - lateral_error/0.005)`
- 문제:
  - gap이 맞아도 lateral 10 mm이면 전체가 `exp(-2)=0.135`로 감소
  - lateral이 맞아도 gap 오차 때문에 lateral 학습 신호가 감소
  - contact까지 곱해져 초기 shaping이 지나치게 결합됨
- 변경 후 gap:
  `contact_gate * exp(-abs(gap-target)/0.005)`
- gap은 Stick2 current local `+y` 장축 성분을 제거한 transverse plane에서 tip 중심
  거리를 구하고, 현재 사각 단면 회전 방향의 두 support radius를 빼서 계산함.
- 변경 후 lateral penalty:
  `-5 * (1-exp(-lateral_error/0.005))`
  - mode와 contact gate에 독립
  - 0 mm에서 0, 5 mm에서 약 -3.16, 10 mm에서 약 -4.32
  - 큰 오차에서도 -5로 포화
- `mode_grasp_stability`는 최종 dense conjunction으로 기존 contact×gap×lateral×quiet를
  유지하고, success도 lateral error `<=5 mm`를 계속 요구함.
- 변경 전 backup:
  `nrmk_isaaclab_wuji/backups/hand_grasp_pre_lateral_split_2026-07-30/`
- smoke:
  `logs/rsl_rl/hand_grasp/2026-07-30_10-28-30`
  에서 `tip_lateral_penalty`를 포함한 reward 12개와 termination 5개가 resolve되고
  24 policy step을 통과함.

## Stick2 고정 병목 진단과 anchor reward

- 분석 run: `logs/rsl_rl/hand_grasp/2026-07-30_10-31-54`
- 초반 약 330 iter에서:
  - ring-tip→Stick2 force: `0.737 N`
  - Stick2 palm-frame position error: `9.72 mm`
  - Stick2 palm-frame orientation error: `0.580 rad` (`33.3°`)
  - Stick2 palm-relative angular speed: `6.45 rad/s`
- 약지 접촉은 충분히 강한데 Stick2 자체가 이동·회전했으므로 ring reward 증량이 아니라
  Stick2를 dynamic reference rail로 유지하는 조건이 필요하다고 판정함.
- 변경 전 backup:
  `nrmk_isaaclab_wuji/backups/hand_grasp_pre_stick2_anchor_2026-07-30/`
- 독립 pose deviation penalties:
  - position: `-10*(1-exp(-e_pos/0.005))`
  - orientation: `-10*(1-exp(-e_ori/(10°)))`
  - 두 항은 contact와 무관하고 각각 `[-10, 0]`으로 bounded임.
- OPEN/CLOSE gap reward와 mode stability에 다음 anchor gate를 곱함:
  `g_anchor=exp(-e_pos/0.005-e_ori/(10°))`.
  따라서 현재 Stick2 자세를 움직여 gap을 맞추는 편법은 contact가 유지돼도 mode shaping을
  거의 받지 못함.
- success hard limit:
  - Stick2 position error `<=5 mm`
  - Stick2 orientation error `<=10°`
- Stick2는 fixed/kinematic으로 만들지 않았고 계속 독립 dynamic rigid body임.
  정책이 palm·엄지 중간마디·약지 접촉으로 실제 고정해야 함.
- command dwell/episode은 `5 s/10 s`, obs/action은 `103D/20D`, entropy `0.001`,
  기존 6-contact acquire/loss latch는 유지함.
- smoke:
  `logs/rsl_rl/hand_grasp/2026-07-30_11-25-34`
  에서 reward 13개와 termination 5개가 resolve되고 24 policy step을 통과함.

## `21-05-32` exact baseline 재학습으로 복귀

- 사용자 결정: 강화 항을 계속 누적하지 않고, 6-contact가 약 2450 iteration까지 유지됐던
  `logs/rsl_rl/hand_grasp/2026-07-29_21-05-32` 구성을 그대로 fresh 재현한 뒤
  좋은 시점에서 수동 중단함.
- 직전 Stick2-anchor 버전 backup:
  `nrmk_isaaclab_wuji/backups/hand_grasp_stick2_anchor_2026-07-30/`
- 원 run 저장 YAML에서 확인한 active 설정:
  - episode `10 s`
  - mode resampling `2 s`
  - action/obs `20D/103D`
  - entropy `0.001`
  - reward 10개: joint 2, pivot 10, Stick2 pose 15, open/close 각 20,
    contact min 20 (`0.10 N` scale), mode stability 50, angular -0.1,
    success 30000, action-rate -0.001
  - termination 4개: timeout, Stick1 drop, Stick2 drop, unreachable success
- 비활성화한 후속 실험:
  6-contact gap gate, contact mean+min 강화, independent lateral penalty,
  Stick2 bounded penalties/anchor gate, contact-collapse termination.
- CLOSE `3 mm`와 success gap tolerance `±3 mm`도 원 run 그대로 유지함.
  끝까지 닫는 `0 mm` 목표는 exact baseline 재현 뒤 별도 단일변수 실험으로 남김.
- smoke `2026-07-30_12-13-39`:
  reward 10개, termination 4개, 24 step 통과.
  원 run YAML과의 diff는 smoke override인 env 1개, iteration 1회, 로그 경로뿐이었음.
- exact 복원 직후 단일 task-definition 변경:
  CLOSE target `3 mm→0 mm`, OPEN/CLOSE 공통 success tolerance `±3 mm→±0.5 mm`.
  dense reward sigma는 `5 mm`로 유지해 목표 근처까지의 shaping은 보존함.

## `12-21-21` 성공 기준선 보존과 장축 보완 A/B

- 현재 가장 잘 된 OPEN/CLOSE run을
  `logs/rsl_rl/hand_grasp/2026-07-30_12-21-21`로 확정함. 약 1.5시간,
  900 iteration까지의 모든 checkpoint, TensorBoard event, exported policy, params,
  git diff를 `backups/runs/hand_grasp/2026-07-30_12-21-21_success/`에 통째로 복사함.
  원본과 백업은 각각 26 MB, run 파일 25개이며 checksum dry-run에서 차이가 없었음.
  백업에는 별도 `BACKUP_INFO.md`를 추가함.
- replay 우선 후보는 `model_550/600/650/850.pt`이며 전체 checkpoint를 삭제 없이 보존함.
  축 보완 전 핵심 소스 4개는
  `nrmk_isaaclab_wuji/backups/hand_grasp_pre_axial_2026-07-30/`에 별도 보존함.
- `12-21-21` 저장 설정은 CLOSE `0 mm`, gap 허용오차 `0.5 mm`,
  episode/dwell `10 s/2 s`, obs/action `103D/20D`, entropy `0.001`임.
  원하는 OPEN/CLOSE는 나왔지만 마지막 100 iteration에서 full-contact, gap error,
  mean reward가 소폭 악화되어 이 run은 더 resume하지 않고 성공 기준선으로 고정함.
- 새 단일변수 A/B는 y 장축 오차를 별도 음수 penalty로 두지 않음. 기존의
  gap+lateral 결합 지수 커널을 그대로 확장함:
  `exp(-e_gap/0.005-e_lateral/0.005-e_axial/0.005)`.
- `e_axial`은 두 distal-tip 중심 차벡터를 현재 Stick2 local `+y` 장축에 투영한 값과
  검증 자세 `pose_005`의 기준 `-4.8016 mm` 사이 절대오차임. 0 정렬을 강요하지 않음.
  같은 항을 OPEN/CLOSE reward와 `mode_grasp_stability`에 넣음.
- TensorBoard 판독용 `tip_axial_offset`, `tip_axial_error`를 추가함.
  별도 reward term, observation, action, PPO, success hard gate는 추가/변경하지 않아
  active reward 수는 기존 10개를 유지함. 새 A/B는 checkpoint resume 없이 fresh로 실행함.

## 장기 8-phase FSM 초안 검토

- 제안 순서 `stick acquire/orient → palm-frame placement → functional grasp →
  object approach → object pinch → transport → release → terminal`은 타당함.
- 각 phase 완료는 순간 threshold가 아니라 일정 step dwell과 hysteresis로 판정함.
  특히 물체 집기는 양 tip contact만으로 끝내지 않고 물체가 tip midpoint를 따라 움직이는
  hold/slip 조건까지 요구함.
- policy observation에는 phase one-hot 8D와 phase elapsed/progress를 넣고, 이전 phase의
  핵심 조건은 다음 phase reward의 gate로 유지함. phase completion 보상은 한 번만 지급함.
- 긴 phase-0 episode 하나로 전부 학습시키지 않고 각 phase의 저장된 유효 초기 상태를
  섞는 phase-conditioned reset/curriculum을 사용함. 현재 `hand_grasp`는 이 구조에서
  phase 2의 파지 유지와 phase 4의 OPEN/CLOSE 동작을 먼저 검증한 micro-skill로 봄.
- FSM 구현은 아직 하지 않았으며 axial A/B 결과 뒤에 phase별 reset state와 전이 threshold를
  확정함.

## Phase 2→3 전용 `hand_setting` 단일 단계 MDP 구현

- `hand_setting` scaffold를 실제 학습 환경으로 전환함. 첫 실험은 사용자가 정한 대로
  thumb-seat/roll/support subphase와 command를 만들지 않고 하나의 policy가 전체 전환을 수행함.
- 초기 상태는 열린 Wuji hand(`finger1_joint2=-0.1659 rad`, 나머지 0)와
  world `x=0.055/0.035, y=0, z=0.5195`에 평행하게 놓인 Stick1/2임.
  두 stick은 reset 뒤 자유롭게 움직이는 dynamic body이며 수동 PD preload는 없음.
- 기존 20D current-joint residual action과 PhysX/contact sensor는 재사용함.
  mode 2D를 제거해 observation은 101D:
  `q20 + qdot20 + fingertip15 + stick pose14 + relative velocity12 + prev action20`.
- reward 설계:
  - final `pose_005`를 향하는 넓은 q/Stick1/Stick2 shaping
  - 네 좁은 semantic link가 올바른 stick의 중앙 shaft로 접근하는 proximity
  - 6접촉 saturated score의 mean(첫 접촉부터 dense)을 동등한 여섯 per-contact
    TensorBoard term으로 분해한 항과 min(6/6 completion)
  - final pose·shaft-region으로 gate한 completion 및 low-slip stability
  - 30-step 최종 성공 pulse와 작은 action-rate penalty
- semantic region은 특정 surface normal이나 단일 contact point를 강제하지 않음.
  correct link-object pair는 contact sensor로 확인하고, 네 좁은 접촉만 스틱 중앙
  160 mm 안인지 검사함. palm/Stick2와 thumb-mid/Stick2는 broad valley anchor이므로
  Stick2 final palm pose와 실제 pair force로 판정함.
- 성공은 six-pair `>=0.02 N`, Stick1 `<=20 mm/20°`, anchor Stick2
  `<=15 mm/20°`, relative speed `<=0.15 m/s/3 rad/s`를 30 policy step 유지하는 조건이며
  만족 즉시 episode를 종료함. episode length는 15초임.
- `2026-07-30_17-05-31` 최종 1-env/1-iteration smoke:
  command 0, observation/action 101D/20D, reward/termination 18/4,
  실제 24 policy step 통과. 넓힌 접근 reward 네 항과 per-contact tag가 정상 resolve됨을 확인함.
- 기존 `hand_grasp`/`12-21-21`은 변경하지 않았으며 hand_setting은 101D라 기존
  103D checkpoint를 resume하지 않음.
- 전체 task import 중 누락되어 있던 Chopsticks acquire의
  `STICK_POS=(0.62,-0.20)`을 기존 주석과 일치하게 복구함.

## Root backup directory 정리

- root `backups/`의 loose code backup을 삭제 없이 task별로 이동함.
  - Box Transport code 5개 → `backups/code/box_transport/`
  - Chopsticks Grasp code 8개 → `backups/code/chopsticks_grasp/`
- 완전한 `12-21-21` hand run은
  `backups/runs/hand_grasp/2026-07-30_12-21-21_success/`로 이동함.
- `backups/README.md`에 code/run/package-local snapshot 분류 기준과 현재 파일 목록,
  향후 naming 규칙을 기록함. 기존 파일명과 run 내부 checkpoint는 변경하거나 삭제하지 않음.

## `hand_grasp` play 키보드 OPEN/CLOSE 선택

- `scripts/rsl_rl/play.py --keyboard_hand_mode` 실행 중 숫자 `1`은 OPEN command `[1,0]`,
  숫자 `2`는 CLOSE command `[0,1]`을 선택함. 초기 모드는 OPEN임.
- Isaac/Carb viewport key callback은 mode index만 thread-safe queue에 넣고 simulation loop가 queue를 비운 뒤
  command tensor와 observation을 갱신함. 따라서 listener thread에서 CUDA tensor를 조작하지 않으며
  키를 누른 다음 policy action부터 새 mode를 입력으로 받음.
- command resampling timer를 멈추고, drop/reset에서 command가 재샘플되어도 마지막 키보드 선택을
  다음 policy observation 전에 복원함.
- Isaac Sim viewport에 포커스를 두고 사용하며 `--headless`와 함께 쓰면 명시적 CLI error를 냄.
- 보조 고정 옵션 `--hand_mode {open,close}`와 기존 자동 교대
  `--alternate_hand_mode`도 유지함. 세 선택 방식을 동시에 사용할 수 없으며 mode·목표 gap·실제
  gap 진단은 자동 출력됨.
- `py_compile`, `git diff --check`, `play.py --help`에서
  `--keyboard_hand_mode` 노출을 확인함. 실행 중 학습을 방해하지 않도록 별도 PhysX replay는 띄우지 않음.

## `hand_setting` TensorBoard metric 추가

- `CustomRewardManager`에 `hand_setting` 전용 metric 설정/계산 경로를 추가함. 기존
  `hand_grasp` success parameter 형식과 달라 metric이 비활성화되던 문제를 분리된
  `_configure_hand_setting_metrics()`로 해결함.
- TensorBoard family는 다음 네 종류임.
  - `Metrics/hand_setting/*`: 에피소드 중 시간 평균
  - `Metrics/hand_setting_final/*`: 종료 직전 값
  - `Metrics/hand_setting_min/*`: 에피소드 중 최솟값
  - `Metrics/hand_setting_max/*`: 에피소드 중 최댓값
- 각 family에 32개 지표를 기록함.
  - exact six-pair force, 최소 force, contact count/fraction, full-contact
  - thumb/index/middle/ring central-shaft region score, region count/fraction/all-valid
  - Stick1/2 palm-frame position/orientation error, 개별/전체 pose-valid
  - Stick1/2 palm-relative linear/angular speed와 각 최대값
  - 최종 성공 gate와 동일한 `setting_valid`, 연속 유지 step `success_stable_steps`
- 계산은 active success/reward config의 sensor group, threshold, pose tolerance,
  region, speed limit를 그대로 재사용하므로 reward/termination과 판정 기준이 어긋나지 않음.
- 실제 smoke `logs/rsl_rl/hand_setting/2026-07-30_17-26-48`를
  `1 env, 1 iteration, episode_length_s=0.2`로 실행해 24 policy step을 통과함.
  TensorBoard event accumulator로 `Metrics/hand_setting*` scalar 128개
  `(32 metrics × 4 families)`가 기록된 것을 확인함.
- 이미 실행 중이던 학습 프로세스는 변경 전 Python class를 메모리에 들고 있으므로 새 metric이
  생기지 않음. 지표 판독은 해당 프로세스를 재시작한 뒤 수행함. metric 외 obs/action/reward/PPO는
  불변이므로 최신 hand_setting checkpoint resume는 호환되며 새 run 폴더로 로그를 분리할 수 있음.
